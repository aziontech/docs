---
interface Props {
	data: Array<{
		title: string,
		cards: Array<{
			icon: string,
			title: string,
			description: string,
			link: string
		}>
	}>;
}

import CardBaseClickable from '~/components/webkit/CardBaseClickable.vue';
import CardTitle from '~/components/webkit/CardTitle.vue';
import CardDescription from '~/components/webkit/CardDescription.vue';

const { data } = Astro.props;
---
<div class="mt-8">
{
	data.map((item) =>(
		<h2>
			{item.title}
		</h2>
		<div class="grid md:grid-cols-2 gap-3 my-8">
			{
				item.cards.map((card) => (
					<CardBaseClickable
						grid={true}
						hover="outlined"
						class="w-full"
						spacing="base"
						link={card.link}
						title={card.title}
					>
						<Fragment slot="content">
							<CardTitle titleTag="h3" class="m-0">
								{card.title}
							</CardTitle>
							<CardDescription>
								{card.description}
							</CardDescription>
						</Fragment>
					</CardBaseClickable>
				))
			}
		</div>
	))
}
</div>
	