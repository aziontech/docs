---
import { useTranslations } from '~/i18n/util';
import { getLanguageFromURL } from '~/util';

import OnThisPage from '~/components/webkit/OnThisPage.vue';

const t = useTranslations(Astro);
const lang = getLanguageFromURL(Astro.url.pathname) || 'en';

const { headings, githubEditUrl } = Astro.props;

const groups = [
	{
		label: 'GitHub',
		links: [
			{
				label: 'Azion Docs',
				href: 'https://github.com/aziontech/docs/blob/main/README.md',
				icon: 'pi pi-github',
				tracking: 'github_link'
			},
			{
				label: t('rightSidebar.editPage'),
				href: githubEditUrl,
				icon: 'pi pi-pencil',
				tracking: 'contribute_link'
			}
		]
	},
	{
		label: t('rightSidebar.community'),
		links: [
			{
				label: t('rightSidebar.joinDiscord'),
				href: 'https://discord.com/invite/Yp9N7RMVZy',
				icon: 'pi pi-discord'
			},
			{
				label: t('rightSidebar.readBlog'),
				href: `https://www.azion.com/${lang}/blog/`,
				icon: 'pi pi-comment'
			},
			{
				label: t('rightSidebar.followTwitter'),
				href: 'https://twitter.com/aziontech',
				icon: 'ai ai-twitter'
			}
		]
	}
];
---

<div class="wrap-nav wrap-nav--island h-full">
	<nav
		class="flex h-full flex-col pt-(--spacing-md) pb-(--spacing-lg)"
		aria-label={t('rightSidebar.a11yTitle')}>
		<OnThisPage
			client:idle
			headings={headings ?? []}
			title={t('rightSidebar.onThisPage')}
			initialHeadingTitle={t('rightSidebar.overview')}
			groups={groups}
		/>
	</nav>
</div>

<style>
	/*
	 * `astro-island` is generated around a hydrated component, so nothing can put a
	 * class on it — and the ScrollArea inside only inherits this column's height once
	 * the island stops being a box in between. The outline scrolls through that
	 * primitive (thin, themed scrollbar) rather than a native `overflow: auto`, which
	 * painted a white scrollbar over the dark canvas.
	 */
	astro-island {
		display: contents;
	}
</style>
