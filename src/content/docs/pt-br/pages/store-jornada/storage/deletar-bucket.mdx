---
title: Como excluir um bucket do Object Storage
description: >-
  Aprenda a excluir os buckets na sua conta usando a API da Azion e a Azion CLI.
permalink: /documentacao/produtos/store/storage/deletar-buckets/
meta_tags: >-
  Object Storage, Storage, Cloud, S3, Bucket, File Storage, Object Storage, API,
  Azion CLI, Delete Buckets
namespace: docs_store_journey_storage_delete_buckets
menu_namespace: storeMenu
---

import Tabs from '~/components/tabs/Tabs'

Este guia orienta você no processo de exclusão de um bucket do Object Storage usando a API da Azion e a Azion CLI.

## Excluindo um bucket

<Tabs client:visible>
    <Fragment slot="tab.cli">Azion CLI</Fragment>
    <Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.cli">

### Requisitos 

- [Azion CLI instalada](/pt-br/documentacao/produtos/azion-cli/visao-geral/#instalar-a-azion-cli). 
- [Um personal token configurado](/pt-br/documentacao/devtools/cli/globals/#token).

Para excluir um bucket:

```bash 
azion delete edge-storage bucket --name yourbucketname
```

</Fragment>

<Fragment slot="panel.api">
 
Execute a seguinte requisição `DELETE` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) e `bucket_name` pelo nome do bucket que você deseja excluir:

```bash
curl --location --request DELETE 'https://api.azion.com/v4/storage/buckets/bucket_name' \
--header 'Accept: application/json' \
--header 'Authorization: Token [TOKEN VALUE]'
```

Você vai receber uma resposta semelhante a esta:

```bash 
/{"state":"executed","data":{"name":"bucketname","edge_access":"read_only"}}% 
```

:::note
Não é possível excluir um bucket que não esteja vazio através da API da Azion. Certifique-se de que o bucket está vazio antes de usar este método para excluí-lo.
:::

</Fragment>

</Tabs>

