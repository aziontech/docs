---
title: Como inserir dados em uma tabela do SQL Database
description: Aprenda como inserir dados em uma tabela do SQL Database usando a API Azion.
permalink: /documentacao/produtos/store/sql/inserir-dados-em-tabela/
meta_tags: SQL Database, SQL, database, CRUD, API, insert data
namespace: docs_store_journey_sql_insert_into_table
menu_namespace: storeMenu
---

Se você já criou um banco de dados, você pode usar o **SQL Database** da Azion para inserir novos dados através da API da Azion.

## Adicione dados a uma tabela

Execute a seguinte requisição `POST` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) e `{id_database}` pelo ID da tabela na qual você deseja inserir dados:

```bash
curl --location 'https://api.azion.com/v4/edge_sql/databases/{id_database}/query' \
--header 'Authorization: Token [TOKEN VALUE]' \
--header 'Content-Type: application/json' \
--data '{
    "statements": [
        "INSERT INTO users VALUES (1, '\''item 1'\'');",
        "INSERT INTO users VALUES (2, '\''item 2'\'');",
        "INSERT INTO users VALUES (3, '\''item 3'\'');"
    ]
}'
```

Você deve receber a seguinte resposta:

```json
{
  "state": "executed",
  "data": [
    {
      "results": {
        "columns": [],
        "rows": []
      }
    },
    {
      "results": {
        "columns": [],
        "rows": []
      }
    },
    {
      "results": {
        "columns": [],
        "rows": []
      }
    }
  ]
}
```
