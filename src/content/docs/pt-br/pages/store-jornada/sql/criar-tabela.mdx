---
title: Como criar uma tabela no SQL Database
description: >-
  Descubra instruções passo a passo para criar uma tabela em um banco de dados SQL Database para gerenciamento eficiente de dados.
permalink: /documentacao/guias/desenvolvimento-de-aplicacoes/dados/criar-tabela/
meta_tags: >-
  SQL Database, banco de dados SQL, criar tabela, gerenciamento de banco de dados, operações CRUD, integração API, data storage, SQL tutorial, guia SQL Database, criação de banco de dados, comandos SQL
namespace: docs_store_journey_sql_create_table
---

import DocButton from '~/components/webkit/DocButton.vue';

Após ter criado seu [banco de dados do SQL Database](/pt-br/documentacao/guias/desenvolvimento-de-aplicacoes/dados/criar-banco-de-dados/), você pode começar a criar tabelas e inserir dados nelas.

<DocButton href="/pt-br/documentacao/store/sql-database/" label="Saiba mais sobre o SQL Database" kind="secondary" size="medium" />

## Crie uma tabela

Execute a seguinte requisição `POST` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [token pessoal](/pt-br/documentacao/guias/plataforma/conta-e-billing/personal-tokens/) e `{id_database}` pelo ID do banco de dados em que você deseja criar uma tabela:

```
curl --location 'https://api.azion.com/v4/edge_sql/databases/{id_database}/query' \
--header 'Authorization: Token [TOKEN VALUE]' \
--header 'Content-Type: application/json' \
--data '{
"statements": [
"CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL);"
]
}'
```

Você deve receber a seguinte resposta:

```json
{
  "state": "executed",
  "data": [
    {
      "results": {
        "columns": [],
        "rows": []
      }
    }
  ]
}
```
