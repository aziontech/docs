---
title: Como importar dados para o Edge SQL
description: >-
    Este guia explica como importar dados para o seu banco de dados usando o Edge SQL Shell.
meta_tags: edge sql, sql, query, crud, dados, banco de dados
namespace: docs_import_data_edgesql
permalink: /documentacao/produtos/guias/importar-dados-edge-sql/
---

import Tabs from '~/components/tabs/Tabs';
import LinkButton from 'azion-webkit/linkbutton';

Este guia explica como importar dados para o seu banco de dados usando o Edge SQL Shell.

## Requisitos

- **Edge SQL Shell instalado**: Certifique-se de que o Edge SQL Shell está devidamente [instalado em seu sistema](/en/documentation/products/store/sql/install-edge-sql-shell/).

- **Banco de dados**: Você precisa ter um banco de dados criado. Para criar um novo banco de dados no Edge SQL Shell, use o comando:
  
  ```sh
  .create <database-name>
  ```

## Importando dados com Edge SQL Shell

<Tabs client:visible sharedStore="Databases">
<fragment slot="tab.file">CSV ou XLSX</fragment>
<fragment slot="tab.kaggle">Kaggle</fragment>
<fragment slot="tab.mysql">MySQL</fragment>
<fragment slot="tab.postgres">Postgres</fragment>
<fragment slot="tab.turso">Turso</fragment>

<fragment slot="panel.file">
<slot name="file" />

1. Selecione um banco de dados.

```shell
.use database-name
```

2. Importe seu arquivo.

```shell
.import file <csv|xlsx> <file_path> <table_name>
```
Para validar a criação de sua tabela você pode usar:

```shell
.tables
```
</fragment>

<fragment slot="panel.kaggle">
<slot name="Kaggle" />

1. Selecione um banco de dados.

```shell
.use database-name
```

2. Importe seu banco de dados kaggle.

```shell
.import kaggle <dataset> <data_name> <table_name>
```
Para validar a criação de sua tabela você pode usar:

```shell
.tables
```
</fragment>

<fragment slot="panel.mysql">
<slot name="MySQL" />

1. Selecione um banco de dados.

```shell
.use database-name
```

2. Importe seu banco de dados MySQL.

```shell
.import mysql <database> <source_table> <table_name>
```
Para validar a criação de sua tabela você pode usar:

```shell
.tables
```
</fragment>

<fragment slot="panel.postgres">
<slot name="Postgres" />

1. Selecione um banco de dados.

```shell
.use database-name
```

2. Importe seu banco de dados Postgres.

```shell
.import postgres <database> <source_table> <table_name>
```
Para validar a criação de sua tabela você pode usar:

```shell
.tables
```
</fragment>

<fragment slot="panel.turso">
<slot name="Turso" />

1. Selecione um banco de dados.

```shell
.use database-name
```

2. Importe seu banco de dados Turso.

```shell
.import turso <database> <source_table> <table_name>
```
Para validar a criação de sua tabela você pode usar:

```shell
.tables
```
</fragment>
</Tabs>

## Importando dados a partir de um arquivo SQL

Para importar dados de um arquivo `.sql` contendo comandos SQL `INSERT`, use o comando `.read` no Edge SQL Shell. Isso executará as instruções SQL contidas no arquivo e preencherá seu banco de dados com os dados especificados.

```shell
.read <file_name>
```