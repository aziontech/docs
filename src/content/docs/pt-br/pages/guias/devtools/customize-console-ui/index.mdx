---
title: Como personalizar a UI do Console Azion usando o Console Kit
description: O Azion Console Kit permite que você crie uma interface Azion personalizada para atender às necessidades do seu negócio. Neste guia, você aprenderá a fazer algumas alterações visuais simples no Console Azion.
meta_tags: edge, console, development, interface, vue, primevue
namespace: documentation_devtools_console_kit_ui
menu_namespace: devtoolsMenu
permalink: /documentacao/produtos/guias/personalizar-ui-console-kit/
---

O **Console Kit** da Azion permite que você crie uma interface Azion personalizada para atender às necessidades do seu negócio. Neste guia, você aprenderá a fazer algumas alterações visuais simples no Console Azion.

:::caution[warning]
Para seguir este guia, você deve primeiro [configurar seu projeto Console Kit](/pt-br/documentacao/devtools/console-kit/#configure-um-projeto) em um repositório bifurcado na sua conta e inicializar o projeto usando o CLI da Azion para executá-lo localmente. Então, você pode trabalhar na alteração da versão bifurcada do Console Azion e, uma vez concluído, entregar o projeto na edge usando o CLI.
:::

---

## Adicionar um componente de UI e criar uma função de rota para navegação

Neste exemplo, você adicionará um novo componente de UI a uma visualização de lista para chamar a atenção para uma etapa importante de um processo de fluxo de trabalho para os usuários do Console.

Neste caso, você adicionará os componentes `InlineMessage` e `PrimeButton` à visualização de lista de **Aplicações Edge** para alertar seus usuários de que eles devem criar um domínio para ver aplicações edge online, com uma ação de botão que direciona os usuários para a rota `create-domain`. Para fazer isso:

1. Abra o projeto Console Kit no seu IDE.
2. Execute `azion dev` para inicializar um servidor de desenvolvimento local.
3. Na pasta `src/views`, localize a visualização de lista de Aplicação Edge e modifique o código conforme segue:


```js title="src/views/EdgeApplications/ListView.vue"
  ...
  import { computed, ref } from 'vue'

  // import statement for the InlineMessage component
  import InlineMessage from 'primevue/inlinemessage'

  // import statement for the PrimeButton component
  import Illustration from '@/assets/svg/illustration-layers.vue'

  // import statement for the router function
  import { useRouter } from 'vue-router'
  ...
```

4. In the const declaration section, create the `router` const to reference the router function:

```js title="src/views/EdgeApplications/ListView.vue"
  ...
  defineOptions({ name: 'list-edge-applications' })

  // reference the router function for the button action
  const router = useRouter()
  ...  
```

5. Create the `navigateToDomains` function that will be executed when the user clicks the `PrimeButton` before closing the `<script>` tag:

```js title="src/views/EdgeApplications/ListView.vue"
  ...
  
  // on push action to redirect user to create-domain route
  function navigateToDomains() {
    router.push({ name: 'create-domain' })
  }
</script>
```

:::tip
You can use this function with the routes defined in the `src/router/routes` folder.
:::

6. Now add the elements to the layout of the list view inside the `<template>` tag:

```html title="src/views/EdgeApplications/ListView.vue"
    ...
    <template #heading>
      <PageHeadingBlock pageTitle="Edge Applications"></PageHeadingBlock>

      <!--- add the InlineMessage component and customize the message with the button --->
      <InlineMessage
        class="w-fit"
        severity="warn"
        >Remember to create a
        <PrimeButton
          link
          size="small"
          class="p-0"
          @click="navigateToDomains"
        >
          Domain
        </PrimeButton>
         and select an edge application to launch online.
      </InlineMessage>
    </template>
    <template #content>
    ...
```

7. On the browser, access the localhost address and navigate to the **Edge Applications** page. You should see the `InlineMessage` element under the heading.
8. After you're done, run `azion deploy` to launch the changes to the edge.

---

import AuthorList from '~/components/AuthorList.astro'

**Contributors**

<AuthorList filePath="src/content/docs/en/pages/guides/devtools/customize-console-ui/index.mdx"/>