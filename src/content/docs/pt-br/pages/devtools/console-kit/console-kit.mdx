---
title: Console Kit
description: O Azion Console Kit é um kit de desenvolvimento front-end usado para criar uma interface personalizada do Console Azion. Desenvolvido com Vue/Vite, PrimeVue e Tailwind, este kit é integrado à plataforma de computação de borda da Azion.
meta_tags: edge, console, desenvolvimento, interface, vue, primevue
namespace: documentation_devtools_console_kit
menu_namespace: devtoolsMenu
permalink: /documentacao/devtools/console-kit/
---

**Console Kit** é um kit de desenvolvimento front-end usado para criar uma interface personalizada do Console Azion. Desenvolvido com os frameworks [Vue/Vite](https://vuejs.org/) e [Tailwind](https://tailwindcss.com/), além da biblioteca de componentes [PrimeVue](https://primevue.org/), este kit é construído sobre a [API da Azion](https://api.azion.com/) e suporta multi-tenancy, permitindo integração da plataforma de edge computing da Azion com as suas próprias APIs.

Você pode usar o Console Kit para fornecer uma experiência personalizada para os desenvolvedores, criando seu próprio tema ou reorganizando os componentes da UI de acordo com as necessidades do seu negócio. O Console Kit possui uma estrutura simples e organizada com separação em camadas de blocos, componentes e serviços, promovendo uma base de código limpa e gerenciável.

O Console Kit está disponível no [repositório do Github](https://github.com/aziontech/azion-console-kit).

---

## Implementação

Verifique a lista completa de requisitos e guias de instalação e configuração no [repositório do Console Kit](https://github.com/aziontech/azion-console-kit?tab=readme-ov-file#requirements) no GitHub.

### Instalar requisitos

1. Instale o **Node.js** fazendo o [download do executável](https://nodejs.org/en).
2. Abra um terminal e execute o seguinte comando para verificar as versões dos pacotes instalados.

```bash
npm -v && node -v
```

:::note
Você pode precisar de uma versão anterior do Node.js. Para fazer isso, execute:

```bash
sudo npm install -g n && n 18.16.1
```
:::

4. Agora execute o seguinte comando para instalar o gerenciador de pacotes [Yarn](https://yarnpkg.com):

```bash
npm install -g yarn
```

### Configurar projeto

1. Inicie seu terminal e clone o projeto através do seguinte comando, certificando-se de ter o [Git](https://git-scm.com/book/en/v2/Getting-Started-Installing-Git) instalado:

```bash
git clone https://github.com/aziontech/azion-console-kit.git
```

2. Navegue até a pasta do console kit:

```bash
cd azion-console-kit
```

3. Execute o seguinte comando para atualizar o projeto:

```bash
yarn install
```

:::note
Este comando deve ser executado periodicamente para garantir que todas as dependências estejam atualizadas.
:::

4. Execute este comando para iniciar o projeto localmente:

```bash
yarn dev --host
```

### Configurar variáveis de ambiente

Para uma configuração mais rápida, gere um [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) no [Real-Time Manager](https://manager.azion.com) da Azion e salve em um arquivo `.env.development`:

```bash
echo 'VITE_PERSONAL_TOKEN=PERSONALTOKEN' > .env.development
```

Por padrão, o Azion Console Kit usa a pilha `STAGE` para se conectar com as APIs da Azion. Para apontar sua aplicação para a pilha `PRODUCTION`, adicione o seguinte comando no arquivo `.env.development`:

```txt title=".env.development"
VITE_ENVIRONMENT='PRODUCTION'
```

### Entregar o Console Kit no edge

O Azion Console Kit pode ser executado nativamente no edge da Azion usando a [Azion CLI](/pt-br/documentacao/produtos/cli/) (versão >= 0.70.0). Siga estes passos:

1. [Baixe e instale a Azion CLI](/en/documentation/products/azion-cli/overview/#installing-azion-cli) usando o método de sua preferência.
2. Configure seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) na CLI:

```bash
azion --token PERSONAL_TOKEN
```

3. Execute o seguinte comando com privilégios `sudo`:

```bash
azion link
```

4. Responda aos prompts conforme necessário.
5. Quando o deploy de sua aplicação finalizar, a CLI entregará um domínio de edge application no formato `https://xxxxxxxxxx.map.azionedge.net` com o projeto Console Kit em execução.

---

import ContributorList from '~/components/ContributorList.astro'

**Contribuidores** <ContributorList>Contributor</ContributorList>
