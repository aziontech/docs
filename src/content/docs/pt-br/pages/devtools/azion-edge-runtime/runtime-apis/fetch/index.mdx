---
title: JavaScript Runtime APIs - Fetch API
description: >-
  Fetch fornece uma interface para buscar recursos a partir da edge network (por
  exemplo, recursos externos na internet).
meta_tags: Fetch
namespace: documentation_products_edge_functions_runtime_apis_javascript_fetch
permalink: /documentacao/devtools/runtime/api-reference/fetch/
---

import FrameBox from '@aziontech/webkit/frame-box'
import ItemList from '@aziontech/webkit/item-list'
import DocItem from '@aziontech/webkit/doc-item'

### Como Funciona

A [Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API) fornece uma interface para buscar recursos a partir da edge network, como recursos externos na internet, por exemplo.

O Fetch fornece uma definição genérica de objetos de [`Request`](https://developer.mozilla.org/pt-BR/docs/Web/API/Request) e [`Response`](https://developer.mozilla.org/pt-BR/docs/Web/API/Response). Isso permite que eles sejam usados para manipular ou modificar _requests_ e _responses_ ou qualquer tipo de caso de uso que possa exigir que você gere suas próprias _responses_ de forma automatizada.

Ele também fornece uma definição para conceitos relacionados como CORS e a semântica de cabeçalho de origem HTTP, substituindo suas definições distintas em outro lugar.

Uma chamada `fetch()` conta para o teto de tempo de relógio da invocação, que é de 5 minutos e inclui espera de I/O. Para esse valor e para o número de chamadas de saída que uma invocação pode fazer, consulte [Limites de Functions](/pt-br/documentacao/build/functions/limites/). Uma única chamada é delimitada por conta própria ao passar um `AbortSignal.timeout()` em `init`: a promise então é rejeitada com um erro cujo `name` é `TimeoutError`, que o código chamador captura.

### Construtor

`fetch() Promise<response>`

### Propriedades

request: `request` `string` - objeto Request ou String que represente a URL para efetuar o fetch.

init: `requestInit`  -  conteúdo da Request.

Os campos de `init` são os que o objeto [Request](/pt-br/documentacao/devtools/runtime/api-reference/request/#propriedades) documenta: `method` para o método da requisição, `headers` para um objeto `Headers`, `body` para o payload e `redirect` para o modo de redirecionamento, entre `follow`, `error` e `manual`.

### Exemplo

```js
addEventListener("fetch", event => {
  return event.respondWith(
    fetch("https://example.com")
  )
})
```

---

## Recursos relacionados

<FrameBox>
<ItemList>
  <DocItem title="fetch na MDN Web Docs" href="https://developer.mozilla.org/en-US/docs/Web/API/fetch">A referência completa de `fetch()`: os parâmetros, a promise que ela devolve e as exceções que ela lança.</DocItem>
</ItemList>
</FrameBox>
