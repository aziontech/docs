---
title: Azion CLI delete
description: >-
  Gerencie suas Applications e recursos com Azion CLI. Inclui comandos para
  Functions, Rules engine, Cache e muito mais.
permalink: /documentacao/devtools/cli/delete/
meta_tags: >-
  Azion CLI, Applications, Functions, Rules engine, Cache settings,
  Domains, Origins, Variables, Personal tokens, Object Storage
namespace: documentation_cli_delete
menu_namespace: cliMenuAlpha
---

Com o comando `azion delete [recurso]` você pode remover:

- [Applications](#applications).
    - [Functions](#functions). 
    - [Rules engine](#rules-engine). 
    - [Cache settings](#cache-settings).
    - [Workload](#workloads).
    - [Connectors](#connectors). 
    - [Variables](#variables).
    - [Personal tokens](#personal-tokens).
    - [Object Storage bucket](#object-storage-bucket).
    - [Object Storage object](#object-storage-object).

:::note
No caso de um ou mais campos obrigatórios não serem informados através das flags específicas, uma mensagem interativa será exibida, solicitando essas informações.
:::

---

## Applications

#### Uso

```bash
azion delete edge-application --application-id 1234
```

#### Flags obrigatórias

##### application-id

A flag `--application-id` é o identificador único da application.

##### cascade

A flag `--cascade` informa que todos os recursos criados pelo comando `azion deploy` devem ser excluídos. Por padrão, o valor é `true`.

#### Flags opcionais

##### help

A opção `--help` exibe mais informações sobre o comando `azion delete edge-application`.

---

## Functions

#### Uso

```bash
azion delete edge-function --function-id <id_da_funcao>
```

#### Flags obrigatórias

##### function-id

A flag `--function-id` é o identificador único da function.

#### Flags opcionais

##### help

A opção `--help` exibe mais informações sobre o comando `azion delete edge-function`.

---

## Rules engine

#### Uso

```bash
 azion delete rules-engine --rule-id <rule-id> --application-id <application-id> --phase <phase>
```

#### Flags obrigatórias

##### rule-id

A flag `--rule-id` especifica o identificador único da regra no Rules Engine.

##### application-id

A flag `--application-id` especifica o identificador único da sua application.

##### phase

A flag `--phase` especifica a fase da regra do Rules Engine. Pode ser `request` ou `response`.

#### Flags opcionais

##### help

A flag `-h` ou `--help` exibe mais informações sobre o comando `azion delete rules-engine`.

---

## Cache settings

#### Uso

```bash
$ azion delete cache-setting --application-id 1673635839 --cache-settings-id 107313
```

#### Flags obrigatórias

##### application-id

A flag `--application-id` define o identificador único para a application.

##### cache-settings-id

A flag `--cache-settings-id` define o identificador único para uma configuração de cache.

#### Flags opcionais

##### help

A opção `--help` exibe mais informações sobre o comando `azion delete cache-setting`.

---

## Workload

#### Descrição

Remove um domínio da biblioteca de domínios com base em um ID fornecido.

#### Uso

```bash
azion delete domain --domain-id 1234
```

#### Flags obrigatórias

##### domain-id

A flag `--domain-id` especifica o identificador único do domínio que você deseja excluir.

#### Flags opcionais

##### help

A opção `-h` ou `--help` exibe mais informações sobre o comando `azion delete domain`.

---

## Connectors

#### Uso

```bash
$ azion delete origin --application-id <application-id> --origin-key <origin-key>
```

#### Flags obrigatórias

##### application-id

A flag `--application-id` define o identificador único para uma application.

##### origin-key

A flag `--origin-key` define o identificador único para uma origem.

#### help

A opção `-h` ou `--help` exibe mais informações sobre a ação `azion delete origin`.

---

## Variables

#### Uso

```bash
  azion delete variables --variable-id 7a187044-4a00-4a4a-93ed-d230900423221f3
```

#### Flags obrigatórias

##### variable-id

A flag `--variable-id` fornece o UUID da variável de ambiente que está sendo excluída.

#### Flags opcionais

##### help

A opção `--help` exibe mais informações sobre o comando `delete`.

---


## Personal tokens

#### Uso

```sh
azion delete personal-token --id xxxxxx-xxxxx-xxxx-xxxxx-xxxx
```

#### Flags obrigatórias 

##### id

A opção `--id` informa o identificador único do personal token que está sendo excluído.

#### Flags opcionais

##### help

A opção `--help` exibe mais informações sobre o subcomando `delete`.

---

## Object Storage bucket

#### Uso

```bash
azion delete edge-storage bucket --name 'bucket-name'
azion delete edge-storage bucket --help
```

#### Flags obrigatórias

##### bucket-name

A flag `--bucket-name` especifica o nome do bucket no Object Storage que você deseja excluir.

#### Flags opcionais

##### help

A opção `-h` ou `--help` exibe mais informações sobre o comando `azion delete edge-storage bucket`.

---

## Object Storage object

#### Uso

```bash
azion delete edge-storage object --bucket-name 'bucket-name'
azion delete edge-storage object --help
```

#### Flags obrigatórias

##### bucket-name

A flag `--bucket-name` especifica o nome do bucket no Object Storage onde está armazenado o objeto que você deseja excluir.

##### object-key

A flag `--object-key` especifica a chave do objeto no bucket no Object Storage que você deseja excluir.

#### Flags opcionais 

##### help

A opção `-h` ou `--help` exibe mais informações sobre o comando `azion delete edge-storage object`.
