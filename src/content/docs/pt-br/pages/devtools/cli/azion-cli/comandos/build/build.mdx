---
title: Azion CLI build
description: Faça a build de sua edge application e prepare o projeto para a implatação no edge.
meta_tags: cli, next, javascript, astro, vue, react, edge computing
namespace: documentation_cli_build
menu_namespace: cliMenuAlpha
permalink: /documentacao/devtools/cli/build/
---

Faça a build de sua edge application localmente e inicie a preparação para a implatação no edge.

:::note
No caso de um ou mais campos obrigatórios não serem informados através das flags específicas, uma mensagem interativa será exibida, solicitando essas informações.
:::

---

## Uso

```bash
  azion build
```

## Flags opcionais

#### help

A opção `--help` exibe mais informações sobre o comando `build`.

#### mode

A opção `--mode` pode ser especificada se for necessário mudar o modo durante o processo de construção, mas não é obrigatória. Os valores esperados são `deliver` ou `compute`.

#### preset

A opção `--preset` pode ser especificada se for necessário alterar o preset durante o processo de criação, mas não é obrigatória. Os valores esperados são:

- Html
- Javascript
- Typescript
- Angular
- Astro
- Hexo
- Next 
- React
- Vue
- static

