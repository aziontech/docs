---
title: Azion CLI sync
description: >-
  Gerencie e sincronize configurações de applications usando Azion CLI.
  Comandos intuitivos para eficácia em tempo real e suporte a múltiplos
  arquivos.
permalink: /documentacao/devtools/cli/sync/
meta_tags: >-
  azion cli, configuração de recursos, sync comando, arquivos azion.json, cli
  file synchronization, help option, config-dir path, programação edge,
  automação azion, gerenciamento de aplicação
namespace: documentation_cli_sync
menu_namespace: cliMenuAlpha
---

Sincronize seu arquivo local contendo a configuração dos recursos de sua aplicação existente com os recursos remotos através da Azion CLI.

## Uso

```bash
azion sync
```

## Flags opcionais

#### config-dir

A opção `--config-dir` é usada para especificar o caminho relativo onde seus arquivos personalizados `azion.json` e `args.json` estão armazenados. O valor padrão é "azion".

#### help

A opção `--help` exibe mais informações sobre o comando `sync`.

#### env

A opção `--env` é usada para especificar o caminho relativo onde seu arquivo `.env` personalizado está armazenado. O padrão é `.edge/.env`.

:::note
Em seu arquivo `.env`, variáveis com nomes contendo as seguintes substrings serão enviadas como **secrets** para o ambiente da Azion:

  - password
  - pwd
  - secret
  - key
  - hash
  - encrypted
  - passoce
  - auth
  - token
  - secret
:::
