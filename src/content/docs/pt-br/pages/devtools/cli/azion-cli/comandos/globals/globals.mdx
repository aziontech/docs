---
title: Opções globais da Azion CLI
description: >-
  Explore os comandos do Azion CLI para efetuar debug, configuração e
  personalização em diversas etapas do desenvolvimento, facilitando processos e
  automação.
permalink: /documentacao/devtools/cli/globals/
meta_tags: >-
  Azion CLI, CLI comandos, configuração Azion, debug logs, versão Azion, comando
  silent, token CLI, personalização CLI, configuração token, automação CLI
namespace: documentation_cli_globals
menu_namespace: cliMenuAlpha
---

Comandos da Azion CLI que podem ser utilizados em qualquer etapa do processo.

---

## version 

```sh 
azion --version
```

A opção `-v` ou `--version` informa qual versão da Azion você tem instalada.

## debug 

```sh 
azion --debug
```

A opção `-d` ou `--debug` exibe logs no nível de depuração.

## config 

A opção `-c` ou `--config` define a pasta de configuração da Azion apenas para o comando atual, sem alterar as configurações persistentes.

Arquivo de configuração criado pela Azion: 

```bash title="~/.azion/settings.toml"
Token = 'token'
UUID = '' //uuid do token
LastCheck = 2024-02-23T11:44:14-03:00
LastVulcanVersion = "x.y.z\n"
AuthorizeMetricsCollection = 1 // 0 - Nao respondido, 1 - Sim, 2 - Não
ClientId = 'xxx'
Email = 'xxxxx@gmail.com'
```

Novo arquivo de configuração criado pelo usuário: 

```bash title="./new-config-file.toml"
Token = 'token'
UUID = '' //uuid do token
LastCheck = 2024-02-23T11:44:14-03:00 
LastVulcanVersion = "x.y.z\n"
AuthorizeMetricsCollection = 1 // 0 - Nao respondido, 1 - Sim, 2 - Não
ClientId = 'xxx'
Email = 'xxxx@gmail.com'
```

Uso:

```sh 
azion --config ./new-config-file.toml
```

## log-level 

```sh 
azion --log-level info
```

A opção `-l` ou `--log-level` informa o nível de log a ser exibido. As opções são:

- `info` (padrão)
- `error`
- `debug`

## silent 

```sh 
azion --silent
```

A opção `-s` ou `--silent` silencia completamente os logs. Normalmente, é usada para fins de automação.

## timeout

```sh
azion --timeout
```

A opção `--timeout` define o tempo limite para as requisições da CLI em segundos, substituindo o valor padrão de 50 segundos.

## token 

```sh 
azion --token seu-token-pessoal
```

A opção `-t` ou `--token` salva um personal token fornecido localmente para autorizar comandos do CLI.

## yes 

```sh 
azion --yes
```

A opção `-y` ou `--yes` responde automaticamente com sim a todas as interações sim/não.

## help

A opção `h` ou `--help` exibe mais informações sobre o comando `azion`.

## no-color 

A opção `--no-color` indica que a saída não deve usar cores.
