---
title: Uma regra que executa a instância em toda requisição
description: >-
  Construa a regra do Rules Engine cujo behavior Run Function entrega toda
  requisição que um firewall recebe a uma instância do Bot Manager.
meta_tags: 'bot manager, rules engine, run function, firewall rule, criteria, request uri, function instance, console, azion cli, api'
namespace: documentation_products_bot_manager_examples_run_on_every_request
permalink: /documentacao/secure/bot-manager/exemplos/executar-em-toda-requisicao/
---

import FrameBox from '@aziontech/webkit/frame-box'
import ItemList from '@aziontech/webkit/item-list'
import DocItem from '@aziontech/webkit/doc-item'
import DocSteps from '@aziontech/webkit/doc-steps'
import DocStep from '@aziontech/webkit/doc-step'
import Tabs from '~/components/webkit/Tabs.vue'

Uma instância de função não pontua nada até que uma regra do [Rules Engine for Firewall](/pt-br/documentacao/plataforma/firewall/rules-engine/) entregue uma requisição a ela. A regra abaixo corresponde a toda requisição que o firewall recebe e executa uma instância do Bot Manager em cada uma delas. Recorra a ela como a regra que coloca Bot Manager no caminho, e sempre que precisar do critério que não pula nenhuma requisição.

```json
{
  "name": "Run Bot Manager on every request",
  "active": true,
  "criteria": [
    [
      {
        "conditional": "if",
        "variable": "${request_uri}",
        "operator": "starts_with",
        "argument": "/"
      }
    ]
  ],
  "behaviors": [
    {
      "type": "run_function",
      "attributes": {
        "value": 15841
      }
    }
  ]
}
```

`value` guarda o id da **instância de função** neste firewall, então substitua `15841` pelo seu. Ele nunca guarda o id da função instalada, que é o mesmo para toda instância dela na conta.

Selecione a interface que você vai usar. O snippet abaixo segue essa escolha.

<Tabs client:visible sharedStore="interface">
<Fragment slot="tab.console">Console</Fragment>
<Fragment slot="tab.cli">CLI</Fragment>
<Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">

<DocSteps>
  <DocStep title="Abra o firewall que carrega a instância">

  Acesse [Azion Console](https://console.azion.com/) > **Firewalls** e selecione esse firewall.

  </DocStep>
  <DocStep title="Selecione a aba Rules Engine" />
  <DocStep title="Selecione + Rule" />
  <DocStep title="Nomeie a regra">

  Informe `Run Bot Manager on every request` como o nome. A descrição é opcional.

  </DocStep>
  <DocStep title="Defina o critério">

  Na seção **Criteria**, selecione `Request Uri` como variável e _starts with_ como operador, e informe `/` como argumento.

  </DocStep>
  <DocStep title="Adicione o behavior Run Function">

  Na seção **Behaviors**, selecione **Run Function**. Isso adiciona um segundo controle, que não tem rótulo e traz o placeholder `Select an function`. Esse controle guarda as instâncias de função deste firewall; selecione a que deve ser executada.

  </DocStep>
  <DocStep title="Selecione Save" />
</DocSteps>

</Fragment>

<Fragment slot="panel.cli">

`azion create firewall-rule` lê a regra inteira de um arquivo JSON. Ele não tem flag para um critério ou um behavior, então salve a regra acima como `rule.json` e depois:

```bash
azion create firewall-rule --firewall-id <firewall-id> --file rule.json
```

```text
Created Firewall Rule with ID 132159
```

Leia a regra de volta e a plataforma terá acrescentado uma chave que ela mesma atribui, `"order": 0`, que é a posição da regra entre as regras daquele firewall.

</Fragment>

<Fragment slot="panel.api">

Salve a regra acima como `rule.json` e envie-a ao firewall que carrega a instância:

```bash
curl --request POST \
  --url https://api.azion.com/v4/workspace/firewalls/<firewall-id>/request_rules \
  --header 'Accept: application/json' \
  --header 'Authorization: Token [TOKEN VALUE]' \
  --header 'Content-Type: application/json' \
  --data @rule.json
```

A API responde `202` e retorna a regra com uma chave que ela atribuiu: o `order` que a regra tem entre as regras daquele firewall, `0` para a primeira. A coleção é `request_rules`.

</Fragment>

</Tabs>

## Como funciona

`criteria` é uma lista de listas, e o behavior recebe a chave `type`. Um arquivo que escreve `name` no lugar é recusado com `Failed to decode the given 'json' file`, uma mensagem que não nomeia nem o campo nem o motivo: o arquivo é JSON válido e o defeito está no formato. O atributo é `value`, e uma regra carrega no máximo um behavior `run_function`, o que Azion Console impõe desabilitando a opção assim que há um na regra. Executar uma segunda instância na mesma requisição exige uma segunda regra.

Baseie o critério na URI da requisição. A variável `${request_args}` com o operador `matches` e `.*` como argumento parece corresponder a tudo e não corresponde: a variável fica vazia sempre que uma requisição não traz query string, e uma variável vazia não corresponde a nada. Uma regra montada assim passa silenciosamente por todo `POST` cujo payload está no corpo. Em Azion Console a variável `Request Uri` não exige mais nada habilitado, enquanto `Header User Agent` e `Request Args` só são oferecidas quando [Web Application Firewall](/pt-br/documentacao/secure/waf/) está habilitado no firewall.

O seletor do behavior lista instâncias, não funções, e apenas as ativas. Uma instância precisa existir antes de esta regra poder nomeá-la, e é por isso que o objeto e a regra são dois snippets, e não um.

## Recursos relacionados

<FrameBox>
<ItemList>
  <DocItem title="Exemplos de Bot Manager" href="/pt-br/documentacao/secure/bot-manager/exemplos/">Veja o resto dos snippets desta coleção, inclusive as instâncias que esta regra executa.</DocItem>
  <DocItem title="Rules Engine for Firewall" href="/pt-br/documentacao/plataforma/firewall/rules-engine/">Os criteria e os behaviors que uma regra de firewall aceita, além dos dois que este snippet usa.</DocItem>
  <DocItem title="Como Bot Manager funciona" href="/pt-br/documentacao/secure/bot-manager/como-funciona/">Onde esta regra fica no caminho que uma requisição percorre, e o que roda depois que ela corresponde.</DocItem>
  <DocItem title="Primeiros passos com Bot Manager" href="/pt-br/documentacao/secure/bot-manager/primeiros-passos/">Os cinco objetos que colocam uma requisição sob inspeção, construídos uma vez na interface que você preferir.</DocItem>
  <DocItem title="Solucionar problemas de Bot Manager" href="/pt-br/documentacao/secure/bot-manager/solucao-de-problemas/">O que ler quando a regra existe e nenhuma requisição é pontuada.</DocItem>
</ItemList>
</FrameBox>
