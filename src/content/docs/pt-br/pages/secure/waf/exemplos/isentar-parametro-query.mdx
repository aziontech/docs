---
title: Isente um parâmetro de query string
description: >-
  Impeça que uma regra interna dispare em um argumento de query string nomeado, e
  deixe-a disparando em todo o resto.
meta_tags: 'waf, exceção, allowed rules, conditions, specific query string name, ID da regra, operator, contains, console, azion cli, api'
namespace: documentation_products_waf_examples_exempt_query_parameter
permalink: /documentacao/secure/waf/exemplos/isentar-parametro-query/
---

import FrameBox from '@aziontech/webkit/frame-box'
import ItemList from '@aziontech/webkit/item-list'
import DocItem from '@aziontech/webkit/doc-item'
import DocSteps from '@aziontech/webkit/doc-steps'
import DocStep from '@aziontech/webkit/doc-step'
import Tabs from '~/components/webkit/Tabs.vue'

Uma exceção tira uma parte de uma requisição do score de uma regra interna. A exceção abaixo impede que a regra `1013`, a regra do apóstrofo, dispare no argumento `q` de `/search`, e a deixa disparando em todo outro argumento, path e header. Recorra a ela quando o Tuning ou o Real-Time Events mostrar um termo de busca legítimo bloqueado por uma única regra.

Selecione a interface que você vai usar. O snippet abaixo segue essa escolha.

<Tabs client:visible sharedStore="interface">
<Fragment slot="tab.console">Console</Fragment>
<Fragment slot="tab.cli">CLI</Fragment>
<Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">

<DocSteps>
  <DocStep title="Abra a página WAF Rules">

  Acesse o [Azion Console](https://console.azion.com/) > **Edge Libraries** > **WAF Rules**.

  </DocStep>
  <DocStep title="Selecione o rule set que bloqueou a requisição" />
  <DocStep title="Abra a aba Allowed Rules e selecione + Allowed Rule" />
  <DocStep title="Selecione a regra a isentar">

  Em **Rule ID**, selecione `1013`.

  </DocStep>
  <DocStep title="Descreva a exceção e restrinja-a a um path">

  Em **Description**, informe `Allow apostrophes in the storefront search term`. Em **Path**, informe `/search`.

  </DocStep>
  <DocStep title="Nomeie a parte da requisição">

  Em **Condition**, selecione _Specific Query String Name_ e informe `q` no campo **Name** que aparece. Em **Operator**, selecione `contains`.

  </DocStep>
  <DocStep title="Salve a exceção" />
</DocSteps>

Para a mesma exceção com todo campo que ela aceita explicado, consulte [Crie uma exceção do WAF](/pt-br/documentacao/guias/seguranca-de-aplicacoes/firewall-e-waf/configurar-waf-allowed-rules/).

</Fragment>

<Fragment slot="panel.cli">

Salve a exceção como `exception.json`:

```json
{
  "rule_id": 1013,
  "name": "Allow apostrophes in the storefront search term",
  "path": "/search",
  "operator": "contains",
  "active": true,
  "conditions": [
    { "match": "specific_query_string_name", "name": "q" }
  ]
}
```

O Azion CLI aceita o mesmo arquivo:

```bash
azion create waf-exceptions --waf-id <waf-id> --file exception.json
```

```text
Created WAF Exception with ID 281875
```

Use `--file` na forma de CLI. No Azion CLI 4.23.0 a flag `--conditions` nunca alcança a requisição: qualquer valor que você passe, o comando envia um array vazio e a API o recusa com `10049 Min Length List Field` apontando para `/data/conditions`.

</Fragment>

<Fragment slot="panel.api">

Salve a exceção como `exception.json`:

```json
{
  "rule_id": 1013,
  "name": "Allow apostrophes in the storefront search term",
  "path": "/search",
  "operator": "contains",
  "active": true,
  "conditions": [
    { "match": "specific_query_string_name", "name": "q" }
  ]
}
```

Envie-a para o rule set que bloqueou a requisição:

```bash
curl --request POST \
  --url https://api.azion.com/v4/workspace/wafs/<waf-id>/exceptions \
  --header 'Accept: application/json' \
  --header 'Authorization: Token [TOKEN VALUE]' \
  --header 'Content-Type: application/json' \
  --data @exception.json
```

A API responde `202` e ecoa a condition sob `data.conditions`, inalterada.

</Fragment>

</Tabs>

## Como funciona

O valor de match decide qual chave extra a condition carrega. `specific_query_string_name` nomeia um argumento, então ele aceita um `name`. A sua contraparte genérica, `any_query_string_name`, aceita apenas `match` e cobre todo argumento da requisição. Duas chaves assumem um valor padrão mais largo do que um falso positivo costuma precisar: `rule_id` assume `0`, que é toda regra interna, e `operator` assume `contains`, que trata `name` como uma substring em vez de um padrão.

## Recursos relacionados

<FrameBox>
<ItemList>
  <DocItem title="Exemplos de WAF" href="/pt-br/documentacao/secure/waf/exemplos/">Navegue pelo resto dos snippets desta coleção.</DocItem>
  <DocItem title="Isente um header de requisição" href="/pt-br/documentacao/secure/waf/exemplos/isentar-header/">O mesmo formato para um header, e a condition que silenciosamente cobre todos eles.</DocItem>
  <DocItem title="WAF Exceptions" href="/pt-br/documentacao/secure/waf/custom-allowed-rules/">Cada campo que uma exceção carrega, e os quinze valores de match que uma condition aceita.</DocItem>
  <DocItem title="Crie uma exceção do WAF" href="/pt-br/documentacao/guias/seguranca-de-aplicacoes/firewall-e-waf/configurar-waf-allowed-rules/">As etapas que constroem esta exceção no Azion Console.</DocItem>
  <DocItem title="Leia o score de uma requisição bloqueada" href="/pt-br/documentacao/secure/waf/exemplos/ler-score-do-bloqueio/">Encontre a regra interna a nomear em `rule_id` antes de escrever a exceção.</DocItem>
</ItemList>
</FrameBox>
