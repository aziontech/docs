---
title: Vincule um rule set no azion.config.js
description: >-
  Declare o WAF rule set e a regra do firewall que o aplica no arquivo de
  configuração, em vez de criar os dois à mão.
meta_tags: 'waf, azion config js, set waf ruleset, waf mode, firewall, rules, criteria, thresholds, azion cli'
namespace: documentation_products_waf_examples_config_file_binding
permalink: /documentacao/secure/waf/exemplos/binding-no-arquivo-de-config/
---

import FrameBox from '@aziontech/webkit/frame-box'
import ItemList from '@aziontech/webkit/item-list'
import DocItem from '@aziontech/webkit/doc-item'

O `azion.config.js` declara o rule set e a regra do firewall que o aplica em um único arquivo, então os dois ficam no repositório em vez de em uma interface. Recorra a ele quando a configuração tiver que passar por revisão junto da aplicação que ela protege. O [Azion CLI](/pt-br/documentacao/devtools/cli/) lê o arquivo e cria o que ele declara.

```javascript
import { defineConfig } from 'azion';

export default defineConfig({
  firewall: {
    name: 'storefront-firewall',
    active: true,
    waf: true,
    rules: [
      {
        name: 'Apply storefront-waf',
        active: true,
        criteria: [
          {
            variable: 'request_uri',
            conditional: 'if',
            operator: 'starts_with',
            argument: '/',
          },
        ],
        behaviors: [
          {
            setWafRuleset: {
              wafMode: 'blocking',
              wafId: '<waf-id>',
            },
          },
        ],
      },
    ],
  },
  waf: [
    {
      name: 'storefront-waf',
      engineSettings: {
        engineVersion: '2021-Q3',
        type: 'score',
        attributes: {
          rulesets: [1],
          thresholds: [
            { threat: 'sql_injection', sensitivity: 'medium' },
            { threat: 'cross_site_scripting', sensitivity: 'medium' },
          ],
        },
      },
    },
  ],
});
```

## Como funciona

Três partes têm que concordar. `waf: true` no firewall liga o módulo, que é o único módulo do Firewall que fica desligado até alguém ligá-lo. O array `waf` declara o rule set e a sensibilidade de cada família de ameaças que ele carrega; uma família que você deixa de fora de `thresholds` não é configurada, então liste toda família que você quer pontuada. `setWafRuleset` é o behavior que os une, e ele aceita exatamente duas chaves, `wafMode` e `wafId`.

`wafMode` aceita `learning` ou `blocking` neste arquivo. Essa primeira grafia é a do próprio arquivo de configuração: o mesmo modo se lê _Logging_ no Azion Console e `logging` em um payload da API. As três nomeiam um único modo, no qual o WAF pontua uma requisição, registra o que correspondeu e a entrega mesmo assim.

## Recursos relacionados

<FrameBox>
<ItemList>
  <DocItem title="Exemplos de WAF" href="/pt-br/documentacao/secure/waf/exemplos/">Navegue pelo resto dos snippets desta coleção.</DocItem>
  <DocItem title="Uma regra que aplica um rule set" href="/pt-br/documentacao/secure/waf/exemplos/aplicar-rule-set/">O mesmo binding como uma chamada de API, com a resposta que ela retorna.</DocItem>
  <DocItem title="azion.config.js" href="/pt-br/documentacao/devtools/cli/azion-config-js/">Cada campo que este arquivo aceita, incluindo os behaviors de firewall que uma regra pode carregar.</DocItem>
  <DocItem title="Azion CLI" href="/pt-br/documentacao/devtools/cli/">Instale o comando que lê este arquivo, e autorize-o na sua conta.</DocItem>
  <DocItem title="Como definir as main settings de um firewall" href="/pt-br/documentacao/guias/seguranca-de-aplicacoes/firewall-e-waf/firewall-definir-main-settings/">Onde fica o interruptor do módulo WAF quando o firewall é configurado à mão.</DocItem>
  <DocItem title="Como WAF funciona" href="/pt-br/documentacao/secure/waf/como-funciona/">O que cada modo faz, e as grafias que o modo carrega em outras superfícies.</DocItem>
</ItemList>
</FrameBox>
