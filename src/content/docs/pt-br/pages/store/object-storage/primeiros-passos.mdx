---
title: Primeiros passos com Object Storage usando Azion Console
description: >-
  Crie o seu primeiro bucket do Object Storage pelo Azion Console, faça upload de
  um objeto e confirme que o objeto está armazenado sob a sua key.
meta_tags: 'object storage, quickstart, first steps, bucket, upload object, azion console, workloads access'
namespace: documentation_products_object_storage_quickstart
permalink: /documentacao/store/object-storage/primeiros-passos/
---

import DocCardGroup from '@aziontech/webkit/doc-card-group'
import DocCard from '@aziontech/webkit/doc-card'
import DocSteps from '@aziontech/webkit/doc-steps'
import DocStep from '@aziontech/webkit/doc-step'

Este guia orienta você a armazenar o seu primeiro objeto no [Object Storage](/pt-br/documentacao/store/object-storage/), pelo Azion Console. No fim, você vai ter:

- O seu primeiro bucket, definido como _Read Only_.
- Um objeto armazenado nesse bucket, endereçado pela sua key.
- O objeto listado no bucket, o que confirma o upload.

Prefere a API? Consulte [Primeiros passos com Object Storage usando a API](/pt-br/documentacao/store/object-storage/primeiros-passos-api/).

Um bucket não é uma pasta pública: um objeto do qual você faz upload não responde a nenhuma requisição vinda da internet enquanto uma aplicação não apontar para ele. O resultado se apoia em três objetos, nesta ordem. O **bucket** é o container de nível mais alto para objetos e buckets não são aninhados. Cada **objeto** dentro dele é um arquivo, a sua key e o seu content type. A key endereça o objeto: uma `/` em uma key faz parte da key, não é uma pasta. Um **connector** do tipo Object Storage, combinado com uma regra do Rules Engine em uma [aplicação](/pt-br/documentacao/plataforma/applications/), é o que envia uma requisição ao bucket. Este guia cria os dois primeiros e para por aí.

---

## Pré-requisitos

- Acesso ao Azion Console. Para entrar, consulte [Como acessar o Azion Console](/pt-br/documentacao/guias/plataforma/conta-e-billing/como-acessar-o-azion-console/).
- Permissão para criar buckets e objetos na sua conta. Para mais informações, consulte [Como gerenciar permissões de times](/pt-br/documentacao/fundamentos/teams-permissions/).
- Um arquivo na sua máquina para fazer upload, como uma imagem.

---

## 1. Crie um bucket

Um bucket guarda os objetos dos quais você faz upload. Este é definido como _Read Only_, o nível de acesso para conteúdo que a Azion Platform entrega sem modificar.

<DocSteps>
  <DocStep title="Abra a lista de buckets">

  Acesse [Azion Console](https://console.azion.com/) > **Object Storage** > **Buckets**.

  </DocStep>
  <DocStep title="Abra o formulário de criação">

  Inicie um novo bucket a partir da lista de **Buckets**. Uma conta que ainda não tem buckets oferece **Bucket** na lista vazia.

  </DocStep>
  <DocStep title="Nomeie o bucket">

  Em **General**, em **Name**, insira um nome de 6 a 63 caracteres, usando letras, números e hífens. O nome não pode começar com `azion`. Ele é único em todas as contas Azion: um nome que outra conta já tem é recusado.

  </DocStep>
  <DocStep title="Mantenha o nível de acesso">

  Em **Settings**, mantenha **Workloads Access** como _Read Only_, o padrão, para que a plataforma leia os objetos, mas não possa modificá-los.

  </DocStep>
  <DocStep title="Selecione Create Bucket" />
</DocSteps>

O bucket aparece em **Buckets**, que lista o seu **Name**, **Size**, **Last Editor** e **Last Modified**. O seu nome fica reservado em todas as contas Azion, então nenhuma outra conta pode criar um bucket com ele.

:::note[nota]
**Workloads Access** decide apenas o que a Azion Platform pode fazer com o bucket. Não restringe a API nem o protocolo S3: uma credencial que carrega capacidade de escrita escreve em um bucket _Read Only_. Para mais informações, consulte [Como o Object Storage funciona](/pt-br/documentacao/store/object-storage/como-funciona/).
:::

---

## 2. Faça upload de um objeto

Um objeto é armazenado sob uma key e a key é como toda interface o endereça depois.

<DocSteps>
  <DocStep title="Abra o bucket">

  Em **Buckets**, selecione o bucket que você criou.

  </DocStep>
  <DocStep title="Adicione o arquivo">

  Selecione **Upload files** ou arraste o arquivo até a área de destino. O nome do arquivo se torna a key do objeto, que tem de 1 a 1.024 caracteres. O Console recusa um arquivo maior que 300 MB.

  </DocStep>
</DocSteps>

O objeto é armazenado no bucket, sob a key retirada do nome do arquivo.

---

## 3. Confirme que o objeto está armazenado

O bucket lista o que guarda, então você pode confirmar o upload sem outra interface.

<DocSteps>
  <DocStep title="Abra a lista de buckets">

  Acesse [Azion Console](https://console.azion.com/) > **Object Storage** > **Buckets**.

  </DocStep>
  <DocStep title="Selecione o bucket">

  Selecione o bucket que você criou.

  </DocStep>
  <DocStep title="Encontre o objeto pela sua key">

  O objeto é listado sob a sua key. Uma key que carrega uma `/`, como `images/logo.svg`, agrupa sob o segmento anterior: esse segmento é um prefix, não uma pasta.

  </DocStep>
</DocSteps>

O seu primeiro objeto está armazenado no Object Storage e endereçado pela sua key. O bucket é alcançável pela Azion API e pelo protocolo S3. Por enquanto, por mais nada.

Para entregar o objeto aos usuários finais, aponte um [connector](/pt-br/documentacao/plataforma/connectors/) do tipo Object Storage para o bucket. Uma regra do Rules Engine na aplicação então envia as requisições que casam para ele. Para mais informações, consulte [Use um bucket do Object Storage como origem](/pt-br/documentacao/guias/desenvolvimento-de-aplicacoes/dados/bucket-como-connector/).

---

## Próximos passos

<DocCardGroup cols={2}>
  <DocCard title="Como o Object Storage funciona" href="/pt-br/documentacao/store/object-storage/como-funciona/" label="O que cada nível de acesso permite e como uma requisição chega até um objeto." />
  <DocCard title="Use um bucket do Object Storage como origem" href="/pt-br/documentacao/guias/desenvolvimento-de-aplicacoes/dados/bucket-como-connector/" label="O connector e a regra que colocam o bucket atrás de uma aplicação." />
  <DocCard title="Upload e download de objetos no Object Storage" href="/pt-br/documentacao/guias/desenvolvimento-de-aplicacoes/dados/upload-e-download-de-objetos-do-bucket/" label="O mesmo upload pela Azion API e dentro de uma function." />
  <DocCard title="Buckets e objetos" href="/pt-br/documentacao/store/object-storage/buckets-e-objetos/" label="Todos os campos que um bucket carrega, todas as operações e as regras de nomenclatura." />
</DocCardGroup>
