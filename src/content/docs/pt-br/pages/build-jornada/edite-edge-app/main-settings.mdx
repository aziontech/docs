---
title: Como definir as configurações principais de uma edge application
description: Personalize as configurações, protocolos de entrega e módulos da sua edge application.
meta_tags: build, edge application, configuração, configurações, entrega, http, https, portas, http3
namespace: docs_guides_build_configure_main_settings
permalink: /documentacao/produtos/guias/build/definir-configuracoes-principais/
menu_namespace: buildMenu
---

Quando você [cria uma edge application com a Azion](/pt-br/documentacao/produtos/comecar-com-um-template/), você tem uma série de configurações que pode modificar. Neste guia, você aprenderá a modificar os protocolos, portas de entrega e o tipo de criptografia que sua aplicação usa para se comunicar com seus usuários finais.

## Via RTM

Nesta seção, você modificará algumas configurações do sua aplicação. Você pode ver se as alterações ocorreram [acessando seu domínio Azion](/pt-br/documentacao/produtos/guias/configurar-dominio/).

1. [Acesse o RTM](https://manager.azion.com).
2. No canto superior esquerdo da página, abra o **Products menu**, representado por três linhas horizontais, e selecione **Edge Application**.
3. Clique na edge application que você deseja configurar. A partir desta tela, você pode alterar o nome da aplicação, as configurações de entrega e quais módulos ela usa.

Os próximos passos irão guiá-lo na edição das configurações de uma edge application boilerplate. Sinta-se à vontade para alterar essas configurações para atender às suas necessidades.

### Delivery protocols

Modifique sua aplicação para suportar os [protocolos de entrega](/pt-br/documentacao/produtos/edge-application/main-settings/#protocolos-de-entrega) disponíveis.

1. Vá para a aba **Main Settings**.
2. Em **Delivery Protocol**, selecion **HTTP & HTTPS**.
3. Clique no botão **Save**.

Após algum tempo de propagação, você poderá acessar sua edge application usando o protocolo HTTPS. Para testar o funcionamento do novo protocolo, acesse o endereço `https://xxxxxxxxxx.map.azionedge.net`. Você deverá acessar a aplicação sob um protocolo mais seguro.

Você também pode fazer a seguinte requisição cURL no seu terminal com o protocolo HTTPS:

```bash
curl -I https://xxxxxxxxxx.map.azionedge.net/
```

Uma resposta `2xx` ou `3xx` significa que sua aplicação está rodando no protocolo HTTPS.

:::note
Aplicações HTTP são automaticamente protegidas pelo certificado SAN da Azion. No entanto, aplicações HTTPS requerem um [certificado personalizado](/pt-br/documentacao/produtos/guias/criar-certificado-digital/).
:::

### Delivery ports

Escolha entre as [portas de entrega](/pt-br/documentacao/produtos/edge-application/main-settings/#portas) suportadas para cada protocolo selecionado.

1. Vá para a aba **Main Settings**.
2. Em **HTTP Ports**, além da porta `80`, selecione a porta `8008`.
3. Em **HTTPS Ports**, além da porta `443`, selecione a porta `8443`.
4. Clique no botão **Save**.

Aguarde alguns minutos para que as alterações se propaguem para o edge e tente acessar sua aplicação usando uma das novas portas. Por exemplo, acesse `https://xxxxxxxxxx.map.azionedge.net:8443` e verifique se sua aplicação está rodando na porta personalizada.

Você também pode fazer a seguinte requisição cURL do seu terminal:

```bash
curl -I https://xxxxxxxxxx.map.azionedge.net:8443
```

Uma resposta bem-sucedida significa que sua aplicação também está rodando na porta `8443`.

### Versão e conjunto de cifras TLS

Especifique a [versão mínima do TLS](/pt-br/documentacao/produtos/edge-application/main-settings/#versão-mínima-de-tls) e o [conjunto de cifras](/pt-br/documentacao/produtos/edge-application/main-settings/#cifras) suportado pela sua aplicação.

1. Vá para a aba **Main Settings**.
2. Em **Minimum TLS version**, selecione **TLS 1.2**.
3. Em **Supported Ciphers list**, selecione **TLSv1.2_2021**.
4. Clique no botão **Save**.

Pode levar algum tempo para propagar suas alterações para o edge. Para verificar se suas alterações ocorreram, você pode inspecionar a página `https://xxxxxxxxxx.map.azionedge.net` usando seu navegador e localizar as configurações de segurança da aplicação. Você também pode [executar o comando DIG](/pt-br/documentacao/produtos/guias/executar-o-comando-dig/) para obter mais informações sobre suas configurações de segurança.

A aplicação ainda pode estar rodando em TLS 1.3, já que você selecionou a versão mínima e não a versão exata do TLS. No entanto, você pode verificar o conjunto de cifras sendo usado contra a [lista de cifras suportadas](/pt-br/documentacao/produtos/edge-application/main-settings/#cifras).

### Módulos

Os [Módulos de Edge Application](/pt-br/documentacao/produtos/edge-application/#modules) podem desbloquear funcionalidades e recursos adicionais para suas aplicações. Veja a [página de preços](https://www.azion.com/pt-br/pricing/) para mais detalhes.

Por enquanto, você ativará dois módulos. Os módulos **Application Acceleration** e **Edge Functions** serão usados quando você [ajustar suas configurações de cache](/pt-br/documentacao/produtos/guias/build/ajustar-cache-settings/), [instanciar uma edge function](/pt-br/documentacao/produtos/guias/build/instanciar-edge-functions/) e [Trabalhar com Rules Engine](/pt-br/documentacao/produtos/guias/build/trabalhar-com-rules-engine/). 

Se você é um [accont owner](/pt-br/documentacao/produtos/guias/users-management/), comece ativando os seguintes módulos para sua conta:

1. [Acesse o RTM](https://manager.azion.com).
2. No canto superior direito, clique no **Account menu**.
3. Selecione **Billing & Subscriptions**.
4. Navegue até a aba **Subscriptions**.
5. Ative **Application Acceleration** e **Edge Functions**.
6. Clique no botão **Save**.

:::note[Opcional]
Você também pode aproveitar esta oportunidade para atualizar seu método de pagamento na aba **Payment Methods**.
:::

Em seguida, para habilitar os módulos para o sua aplicação:

7. No canto superior esquerdo da página, abra o **Products menu**, representado por três linhas horizontais, e selecione **Edge Application**.
8. Clique na edge application que você deseja configurar.
9. Ative os módulos **Application Acceleration** e **Edge Functions**.
10. Clique no botão **Save**.

## Via API

1. Execute a seguinte requisição `GET` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) para recuperar seu `<edge_application_id>`:

```bash
curl --location 'https://api.azionapi.net/edge_applications \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]'
```

2. Você receberá uma resposta com todas sua edge applications existentes. Copie o valor do `<edge_application_id>` que você deseja configurar.
3. Execute uma requisição `PATCH` para modificar a aplicação da seguinte forma:

```bash
curl --location --request PATCH 'https://api.azionapi.net/edge_applications/<edge_application_id>' \
--header 'Accept: application/json; version=3' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token [TOKEN VALUE]' \
--data '{
    "delivery_protocol": "http,https",
    "http_port": [80, 8008],
    "https_port": [443, 8443],
    "minimum_tls_version": "tls_1_2",
    "supported_ciphers": "TLSv1.2_2021",
    "edge_functions": true,
    "application_acceleration": true
}'
```


  | Chave | Descrição |
  | --- | --- |
  | `delivery_protocol` | Quando definido como `http,https`, habilita o suporte ao protocolo HTTPS. |
  | `http_port` | Define portas HTTPS para a aplicação. Neste caso, o array `[80, 8008]` define que ambas as portas 80 e 8008 podem ser usadas por solicitações HTTP. |
  | `https_port` | Define portas HTTPS para a aplicação. Neste caso, o array `[443, 8443]` define que ambas as portas 443 e 8443 podem ser usadas por solicitações HTTP. |
  | `minimum_tls_version` | Quando definido como `tls_1_2`, estabelece que a versão mínima do TLS deve ser 1.2. |
  | `supported_ciphers` | Define o conjunto de cifras suportado, sendo seu valor neste caso `TLSv1.2_2021`. Veja a lista completa de [lista de cifras suportadas](/pt-br/documentacao/produtos/edge-application/main-settings/#cifras) para mais detalhes.
  | `edge_functions` | Habilita o módulo **Edge Functions**. |
  | `application_acceleration` | Habilita o módulo **Application Acceleration**. |

4. Você receberá uma resposta semelhante a esta:

```json
{
    "results": {
        "id": <edge_application_id>,
        "name": "example.org",
        "delivery_protocol": "http,https",
        "http_port": [
            80,
            8008
        ],
        "https_port": [
            443,
            8443
        ],
        "minimum_tls_version": "tls_1_2",
        "active": true,
        "debug_rules": false,
        "http3": false,
        "websocket": false,
        "supported_ciphers": "TLSv1.2_2021",
        "application_acceleration": true,
        "caching": true,
        "device_detection": false,
        "edge_firewall": false,
        "edge_functions": true,
        "image_optimization": false,
        "l2_caching": false,
        "load_balancer": false,
        "raw_logs": false,
        "web_application_firewall": false
    }
}
```

5. Aguarde alguns minutos para que as alterações se propaguem.
6. Teste suas alterações acessando sua aplicação usando o protocolo HTTPS e as portas personalizadas configuradas acima. Você pode acessar sua aplicação através de um navegador usando o endereço `https://xxxxxxxxxx.map.azionedge.net:8443` e inspecionar a página ou usar o seguinte comando cURL para uma resposta de status `2xx` ou `3xx`:

```bash
curl -I https://xxxxxxxxxx.map.azionedge.net:8443
```

7. Verifique o conjunto de cifras suportado inspecionando a página ou [executando o comando DIG](/pt-br/documentacao/produtos/guias/executar-o-comando-dig/).

:::tip
Confira a [documentação da API da Azion](https://api.azion.com/) e a [especificação OpenAPI](https://github.com/aziontech/azionapi-openapi/) para saber mais sobre o que a API da Azion pode oferecer.
:::



---

import ContributorList from '~/components/ContributorList.astro'

**Contribuidores** <ContributorList>Contributor</ContributorList>
