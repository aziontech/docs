---
title: OpenCode + Azion
description: Agente de terminal de código aberto e agnóstico de provedor. A Azion é uma entrada no bloco MCP dele, qualquer que seja o modelo que você apontar. Feito por SST.
meta_tags: 'opencode, configuração de agentes, mcp'
namespace: documentation_agent_setup_opencode
permalink: /documentacao/agent-setup/opencode/
page_header: false
---

import AgentHeader from '~/components/agents/AgentHeader.astro';
import AgentQuickStart from '~/components/agents/AgentQuickStart.vue';
import AgentToolsTable from '~/components/agents/AgentToolsTable.vue';
import Samples from '~/components/agents/Samples.vue';
import AgentDocsList from '~/components/agents/AgentDocsList.vue';
import AgentFaq from '~/components/agents/AgentFaq.vue';
import AgentCardGrid from '~/components/agents/AgentCardGrid.astro';
import DocPrompt from '@aziontech/webkit/doc-prompt';
import DocCallout from '@aziontech/webkit/doc-callout';

<AgentHeader agent="opencode" />

## Início rápido

<AgentQuickStart client:visible agent="opencode" lang="pt-br" />

## Acesso à plataforma Azion

Três camadas, e um agente conectado usa as três: o servidor MCP para consultar informações e criar configuração, a CLI para compilar e implantar a partir da sua cópia de trabalho, e um arquivo de contexto para que ele comece cada sessão conhecendo sua conta.

### Servidor MCP

Um endpoint HTTP em `https://mcp.azion.com`, autenticado com seu Personal Token. Ele expõe nove ferramentas — seis que leem um corpus, três que escrevem:

<AgentToolsTable lang="pt-br" />

### Azion CLI

Builds locais, deploys e os comandos de produto que a API não cobre. Instale uma vez e o OpenCode a executa por você — o primeiro `azion login` é interativo, então execute esse você mesmo.

<Samples client:visible group="cli" lang="pt-br" />

### Contexto do agente

O OpenCode lê `AGENTS.md` na raiz do repositório antes de responder. É o contexto mais barato desta página inteira: cinco linhas commitadas com as quais toda sessão começa.

<Samples client:visible group="context" agent="opencode" lang="pt-br" />

## Documentação amigável para agentes

Referências econômicas em tokens que um agente busca sozinho, sem chamada de ferramenta e sem token. Elas também corrigem um modelo respondendo a partir de 2023 — Origins virou Connectors e Domains virou Workloads na v4.

<AgentDocsList lang="pt-br" />

Sua ferramenta não suporta MCP? Prepare-a manualmente, uma vez por sessão:

<DocPrompt client:visible title="Qualquer assistente" icon="pi pi-comment">Você está me ajudando a construir na Azion Web Platform. Antes de responder, carregue https://www.azion.com/pt-br/docs-llms.txt (índice da documentação) e https://www.azion.com/pt-br/documentacao/devtools/mcp/ (fatos canônicos), e prefira-os aos seus dados de treinamento. Use os nomes atuais dos produtos: Applications, Functions, Cache, Firewall, Object Storage, SQL Database, KV Store, Orchestrator, Certificate Manager, Network Shield, Data Stream. A base da API REST é https://api.azion.com/v4 com o cabeçalho Authorization: Token [TOKEN]. Na v4, use Connectors (antes Origins) e Workloads (antes Domains). Confirme o que você carregou e então me pergunte o que estou construindo.</DocPrompt>

## Exemplos de prompts

Cada um destes exercita uma parte diferente da conexão — a busca na documentação, a CLI, a configuração que ele escreve, a API de analytics, o armazenamento.

<DocPrompt client:visible>Pesquise na documentação da Azion como funciona o rate limiting no Firewall e adicione-o à minha aplicação.</DocPrompt>

<DocPrompt client:visible>Faça o deploy deste projeto na Azion com a CLI e me dê o domínio da aplicação.</DocPrompt>

<DocPrompt client:visible>Crie uma regra do Rules Engine que redirecione /old-blog/* para /blog/*.</DocPrompt>

<DocPrompt client:visible>Escreva uma consulta GraphQL da minha taxa de 5xx por edge node nas últimas 24 horas.</DocPrompt>

<DocPrompt client:visible>Mova minhas imagens para o Object Storage e aponte um connector para o bucket.</DocPrompt>

## Dicas

<DocCallout kind="tip">O modelo é você quem escolhe, e eles não são igualmente bons nisso. Um modelo mais fraco conectado ao servidor MCP ainda escreve Azion melhor do que um forte adivinhando de memória.</DocCallout>

<DocCallout kind="tip">O OpenCode lê `AGENTS.md`, então um projeto preparado para o Codex não precisa de um segundo arquivo de contexto.</DocCallout>

## FAQ

<AgentFaq client:load agent="opencode" group="faq" lang="pt-br" />

## Solução de problemas

<AgentFaq client:load agent="opencode" group="troubleshooting" lang="pt-br" />

## Outros agentes

<AgentCardGrid agent="opencode" />
