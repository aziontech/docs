---
title: Azion CLI
description: Através da Azion CLI, você pode construir e realizar o deploy de edge applications utilizando a linha de comando.
namespace: documentation_azion_cli_overview
menu_namespace: cliMenuAlpha
permalink: /documentacao/produtos/azion-cli/visao-geral/
---

import Button from '~/components/Button.astro'


A **Azion CLI** é uma interface de linha de comando (CLI) [open source](https://github.com/aziontech/azion-CLI/) que permite interagir com a plataforma de Edge da Azion pelo terminal. Você pode usar a Azion CLI para:

- Inicializar, criar e implantar edge applications.
- Criar aplicações Jamstack.
- Gerenciar as edge applications na plataforma.
- Ter um servidor de desenvolvimento local em execução.
- Vincular um projeto existente a uma edge application da Azion.

A Azion disponibiliza um conjunto de opções para interagir com os produtos da plataforma, tais como:

- [Azion Terraform Provider](/pt-br/documentacao/produtos/terraform-provider/)
- [Azion API](https://api.azion.com/)
- [Azion SDK (Go)](/pt-br/documentacao/devtools/sdk/go/)

A Azion CLI é desenvolvida em Go e interage com os produtos da Azion através do [Azion SDK (Go)](/pt-br/documentacao/devtools/sdk/go/).

## Instalar a Azion CLI

### Download

Se você optar pelos gerenciador de pacotes **RPM**, **Dpkg** ou **apk**, vá para a [página de lançamentos](https://github.com/aziontech/azion-cli/releases) e faça o download do pacote desejado.

:::note 
Se você escolher usar o Homebrew, a etapa anterior não será necessária.
:::

Escolha uma das seguintes opções:

import Tabs from '~/components/tabs/Tabs';

<Tabs client:visible sharedStore="package-managers">
<Fragment slot="tab.rpm">RPM</Fragment>
<Fragment slot="tab.dpkg">Dpkg</Fragment>
<Fragment slot="tab.apk">APK</Fragment>
<Fragment slot="tab.brew">Brew</Fragment>

<Fragment slot="panel.rpm">
<slot name="rpm" />
```shell
sudo rpm -i <arquivo_baixado>
```
</Fragment>
<Fragment slot="panel.dpkg">
<slot name="dpkg" />
```shell
sudo dpkg -i <arquivo_baixado>
```
</Fragment>
<Fragment slot="panel.apk">
<slot name="apk" />
```shell
apk add <arquivo_baixado>
```
</Fragment>
<Fragment slot="panel.brew">
<slot name="brew" />
```shell
brew install aziontech/tap/azion
```
</Fragment>
</Tabs>

### Alternativas

Você também pode [instalar a Azion CLI manualmente](/pt-br/documentacao/produtos/guias/cli-instalando-manualmente/).

Se você estiver instalando o CLI em um sistema operacional macOS, consulte [como executar a Azion CLI no macOS](/pt-br/documentacao/produtos/guias/cli-executando-macos/).

---

## Atualizar a Azion CLI

Toda vez que uma nova versão é lançada e qualquer comando da Azion CLI é utilizado, um prompt informará o usuário sobre a nova versão e perguntará se deseja atualizá-la ou não.

---

## Opções globais

### version 

```sh 
azion --version
```

A opção `-v` ou `--version` informa qual versão da Azion você tem instalada.

### debug 

```sh 
azion --debug
```

A opção `-d` ou `--debug` exibe logs no nível de depuração.

### config 

```sh 
azion --config
```

A opção `-c` ou `--config` define a pasta de configuração da Azion apenas para o comando atual, sem alterar as configurações persistentes.

### log-level 

```sh 
azion --log-level info
```

A opção `-l` ou `--log-level` informa o nível de log a ser exibido. As opções são:

- `info` (padrão)
- `error`
- `debug`

### silent 

```sh 
azion --silent
```

A opção `-s` ou `--silent` silencia completamente os logs. Normalmente, é usada para fins de automação.

### token 

```sh 
azion --token seu-token-pessoal
```

A opção `-t` ou `--token` salva um token pessoal fornecido localmente para autorizar comandos do CLI.

### yes 

```sh 
azion --yes
```

A opção `-y` ou `--yes` responde automaticamente com sim a todas as interações sim/não.

#### help

A opção `h` ou `--help` exibe mais informações sobre o comando `azion`.

---

<Button href="/pt-br/documentacao/produtos/cli/visao-geral/" text="versões anteriores do Azion CLI 1.0.0" variant="secondary"> 
</Button>
