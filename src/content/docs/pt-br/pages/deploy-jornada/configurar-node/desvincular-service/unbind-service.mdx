---
title: Como desvincular um edge service
description: Desvincule um service de um edge node para seus recursos não serem mais provisionados.
meta_tags: deploy, orchestrator
namespace: docs_deploy_unbind_service
menu_namespace: deployMenu
og_image: ''
permalink: /documentacao/produtos/guias/deploy/desvincular-servico/
---

import Tabs from '~/components/tabs/Tabs'

:::caution[importante]
A Azion possui duas interfaces de usuário: Real-Time Manager e Console, que está em estágio de *Preview*. Atualmente, o Console está disponível apenas para planos de Developer e novas contas. Siga os passos de acordo com a interface de usuário que estiver utilizando.
:::

Você pode desvincular um edge service de um edge node usando:

- [Azion Console](/pt-br/documentacao/produtos/guias/como-acessar-o-azion-console)
- [Azion Real-Time Manager (RTM)](https://manager.azion.com/)
- [Azion API](https://api.azion.com/#5927138a-5150-4fe2-8364-741a3b5857bf)

---

<Tabs client:visible>
    <Fragment slot="tab.console">Console</Fragment>
    <Fragment slot="tab.rtm">Real-Time Manager</Fragment>
    <Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">
1. Acesse o [Azion Console](/pt-br/documentacao/produtos/guias/como-acessar-o-azion-console/) > **Edge Nodes**.
2. Selecione o edge node do qual você deseja desvincular um serviço.
3. Vá para a aba **Services**.
4. Encontre o serviço que você deseja desvincular.
5. Clique no ícone de lixeira.
6. Confirme a desvinculação no modal.
</Fragment>

<Fragment slot="panel.rtm">
1. Acesse o [Real-Time Manager (RTM)](https://manager.azion.com/).
2. No canto superior esquerdo da página, abra o **Products menu**, representado por três linhas horizontais, e então selecione **Edge Nodes**.
3. Selecione o edge node do qual você deseja desvincular um serviço.
4. Vá para a aba **Services**.
5. Passe o mouse sobre o serviço na lista e clique no ícone da lixeira.
6. Confirme a desvinculação no modal.
</Fragment>

<Fragment slot="panel.api">
1. Execute a seguinte requisição `GET` no seu terminal, substituindo `[TOKEN VALUE]` com seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) para recuperar o ID do edge node do qual deseja desvincular o edge service:

```bash 
curl --location 'https://api.azionapi.net/edge_nodes/' \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]'
```

2. Execute a seguinte requisição `LIST` no seu terminal, substituindo `[TOKEN VALUE]` com seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/) e `:id` pelo ID do edge node para obter o ID da relação entre o edge node e o serviço escolhido:

```bash
curl --location 'https://api.azionapi.net/edge_nodes/:id/services' \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]'
```

3. Execute a seguinte requisição `DELETE` no seu terminal, substituindo `[TOKEN VALUE]` com seu [personal token](/pt-br/documentacao/produtos/guias/personal-tokens/), `:node_id` pelo ID do edge node e `:bind_id` com o ID da relação entre o edge node e o edge service:

```bash
curl --location --request DELETE 'https://api.azionapi.net/edge_nodes/:node_id/services/:bind_id' \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]'
```

Agora, seu edge node não tem mais uma relação com este edge service específico. 

:::tip
Consulte a [documentação da API da Azion](https://api.azion.com/) e a [especificação OpenAPI](https://github.com/aziontech/azionapi-openapi/) para saber mais sobre todos os recursos disponíveis via API.
:::
</Fragment>

</Tabs>