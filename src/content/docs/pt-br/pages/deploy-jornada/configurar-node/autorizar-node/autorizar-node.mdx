---
title: Como autorizar um edge node
description: >-
  Saiba como autorizar um Edge Node na Azion usando Console, Real-Time Manager
  ou API Azion. Acesse recursos de Orchestrator eficientemente.
permalink: /documentacao/produtos/guias/deploy/autorizar-edge-node/
meta_tags: >-
  Azion, Edge Node, Autorização, Real-Time Manager, Console Preview, API Azion,
  Orchestrator, OpenAPI
namespace: docs_deploy_authorize_node
menu_namespace: deployMenu
---

import Button from 'azion-webkit/components/Button';
import Tabs from '~/components/tabs/Tabs'


Se você já instalou o [Azion Orchestrator Agent]() no seu dispositivo, o próximo passo é autorizar o edge node na plataforma. Você pode fazer isso usando:

- [Azion Console](/pt-br/documentacao/produtos/guias/como-acessar-o-azion-console/)
- [API Azion](https://api.azion.com)

<Button href="/pt-br/documentacao/produtos/guias/deploy/instalar-orchestrator-agent/" label="instalar o orchestrator agent" type="primary" theme="light" />

<Tabs client:visible>
    <Fragment slot="tab.console">Console</Fragment>
    <Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">
1. Acesse o [Azion Console](/pt-br/documentacao/produtos/guias/como-acessar-o-azion-console/).
2. No canto superior esquerdo da página, abra o **Products menu**, representado por três linhas horizontais, e em seguida selecione **Edge Nodes**.
3. Encontre o edge node que você deseja autorizar, que terá o status `Waiting Authorization`.
4. Clique no ícone de três pontos e depois clique em **Authorize**.
5. Confirme a autorização.
</Fragment>

<Fragment slot="panel.api">
1. Execute a seguinte requisição `GET` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [token pessoal](/pt-br/documentacao/produtos/guias/personal-tokens/):

```bash 
curl --location 'https://api.azionapi.net/edge_nodes/' \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]'
```

2. Execute a seguinte requisição `PATCH` no seu terminal, substituindo `[TOKEN VALUE]` pelo seu [token pessoal](/pt-br/documentacao/produtos/guias/personal-tokens/) e informando o ID do edge node:

```bash 
curl --location --request PATCH 'https://api.azionapi.net/edge_nodes/authorize' \
--header 'Accept: application/json; version=3' \
--header 'Authorization: Token [TOKEN VALUE]' \
--header 'Content-Type: application/json' \
--header 'accept: application/json; version=3' \
--data '{
    "edge_node_ids": [
        0
    ]
}'
```

Agora, seu edge node está autorizado e pode trabalhar com [edge services](/en/documentation/products/deploy/edge-orchestrator/edge-services/). 

:::tip
Confira a [documentação da API Azion](https://api.azion.com/) e a [especificação OpenAPI](https://github.com/aziontech/azionapi-openapi/) para saber mais sobre todos os recursos disponíveis via API.
:::
</Fragment>

</Tabs>
