---
title: How to retrieve data from tables in Edge SQL
description: Learn how to efficiently list and retrieve data from Edge SQL databases using the Azion REST API. This comprehensive guide provides step-by-step instructions for querying tables and optimizing your database management.
meta_tags: 'Edge SQL, SQL query, list data, retrieve data, database management, Azion REST API, SQL tutorial, data retrieval, database optimization, querying tables'
namespace: docs_store_journey_sql_create_table
permalink: /documentation/products/store/sql/retrieve-data-edge-sql/
---

import LinkButton from 'azion-webkit/linkbutton'

Once you've created your Edge SQL database, you can begin creating tables and inserting data into them.

<LinkButton link="/en/documentation/products/store/edge-sql/" label="go to Edge SQL reference" severity="secondary" />

## Listing data in a table

Run the following POST request in your terminal, replacing [TOKEN VALUE] with your personal token and {id_database} with the ID of the table you want to retrieve:

```bash
curl --location 'https://api.azion.com/v4/edge_sql/databases/{id_database}/query' \
--header 'Authorization: Token [TOKEN VALUE]' \
--header 'Content-Type: application/json' \
--data '{
    "statements": [
        "SELECT * FROM users;"
    ]
}'
```

You should receive the following response:

```json
{
  "state": "executed",
  "data": [
    {
      "results": {
        "columns": [
          "id",
          "name"
        ],
        "rows": [
          [
            1,
            "item 1"
          ],
          [
            2,
            "item 2"
          ],
          [
            3,
            "item 3"
          ]
        ]
      }
    }
  ]
}
```