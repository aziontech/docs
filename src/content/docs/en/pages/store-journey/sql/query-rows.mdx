---
title: How to retrieve data from tables in SQL Database
description: This comprehensive guide provides step-by-step instructions to efficiently list and retrieve data from SQL Database databases using the Azion REST API. 
meta_tags: 'SQL Database, SQL query, list data, retrieve data, database management, Azion REST API, SQL tutorial, data retrieval, database optimization, querying tables'
namespace: docs_store_journey_sql_query_rows
permalink: /documentation/products/store/sql/retrieve-data-sql-database/
menu_namespace: storeMenu
---

import Button from 'azion-webkit/components/Button';

Once you've created your SQL Database database, you can begin retrieving data from them.

<Button href="/en/documentation/products/store/edge-sql/" label="go to SQL Database reference" type="primary" theme="light" />

## Listing data in a table

Run the following POST request in your terminal, replacing [TOKEN VALUE] with your personal token and `{id_database}` with the ID of the table you want to retrieve:

```bash
curl --location 'https://api.azion.com/v4/edge_sql/databases/{id_database}/query' \
--header 'Authorization: Token [TOKEN VALUE]' \
--header 'Content-Type: application/json' \
--data '{
    "statements": [
        "SELECT * FROM users;"
    ]
}'
```

You should receive the following response:

```json
{
  "state": "executed",
  "data": [
    {
      "results": {
        "columns": [
          "id",
          "name"
        ],
        "rows": [
          [
            1,
            "item 1"
          ],
          [
            2,
            "item 2"
          ],
          [
            3,
            "item 3"
          ]
        ]
      }
    }
  ]
}
```
