---
title: Claude Code + Azion
description: Terminal agent that reads your codebase, runs commands, and edits files. One CLI command connects the Azion MCP server. Made by Anthropic.
meta_tags: 'claude code, agent setup, mcp'
namespace: documentation_agent_setup_claude_code
permalink: /documentation/agent-setup/claude-code/
agent:
  mark: claude
  vendor: Anthropic
  tags:
    - Terminal
    - Subscription
    - Locked
    - Project memory
  links:
    - label: Azion MCP server
      href: /en/documentation/devtools/mcp/
    - label: Azion CLI
      href: /en/documentation/devtools/cli/
    - label: Claude Code docs
      href: https://docs.anthropic.com/en/docs/claude-code
---

import AgentFaq from '~/components/webkit/AgentFaq.vue';
import AgentQuickStart from '~/components/webkit/AgentQuickStart.vue';
import AgentSamples from '~/components/webkit/AgentSamples.vue';
import AgentDocs from '~/includes/agent-setup/en/agent-docs.mdx';
import CliSamples from '~/includes/agent-setup/en/cli-samples.mdx';
import McpTools from '~/includes/agent-setup/en/mcp-tools.mdx';
import CursorCard from '~/includes/agent-setup/en/cards/cursor.mdx';
import GithubCopilotCard from '~/includes/agent-setup/en/cards/github-copilot.mdx';
import WindsurfCard from '~/includes/agent-setup/en/cards/windsurf.mdx';
import CodexCard from '~/includes/agent-setup/en/cards/codex.mdx';
import GeminiCliCard from '~/includes/agent-setup/en/cards/gemini-cli.mdx';
import OpencodeCard from '~/includes/agent-setup/en/cards/opencode.mdx';
import DocCardGroup from '@aziontech/webkit/doc-card-group';
import DocPrompt from '@aziontech/webkit/doc-prompt';
import DocCallout from '@aziontech/webkit/doc-callout';

## Quick start

<AgentQuickStart client:visible
	tryIt="Try it"
	tooltips={{
		"mcp": {
			"headline": "MCP",
			"tip": "Model Context Protocol — the open standard an agent uses to call tools it did not ship with. Azion exposes nine of them over one authenticated HTTP endpoint.",
			"cta": "See the nine tools",
			"href": "#mcp-server"
		},
		"token": {
			"headline": "Personal Token",
			"tip": "Your own API credential, scoped to the permissions you choose and revocable at any time. The agent authenticates as you with it — it never gets one of its own.",
			"cta": "Console › Personal Tokens",
			"href": "https://console.azion.com/personal-tokens"
		}
	}}
	steps={[
		{
			"key": "install",
			"title": "Install Claude Code",
			"body": "Install the CLI globally, then run it once from your project so it can index the repository.",
			"samples": [
				{
					"label": "npm",
					"language": "bash",
					"code": "npm install -g @anthropic-ai/claude-code"
				}
			]
		},
		{
			"key": "token",
			"title": "Create a Personal Token",
			"body": "The [[mcp:MCP server]] authenticates as you. Create a [[token:Personal Token]] in the Console, give it only the scopes you want Claude Code to have, and copy it — it is shown once.",
			"link": {
				"label": "Console › Personal Tokens",
				"href": "https://console.azion.com/personal-tokens"
			}
		},
		{
			"key": "connect",
			"title": "Connect the Azion MCP server",
			"body": "One command registers the server for every session. Claude Code stores it in your user configuration, so it is not a file you commit.",
			"samples": [
				{
					"label": "Terminal",
					"language": "bash",
					"code": "claude mcp add \"azion-mcp\" \"https://mcp.azion.com\" -t http -H \"Authorization: Bearer YOUR_PERSONAL_TOKEN\""
				}
			]
		},
		{
			"key": "verify",
			"title": "Verify, then try a prompt",
			"body": "`claude mcp list` names the servers it knows about; `/mcp` inside a session lists the tools each one exposes.",
			"samples": [
				{
					"label": "Terminal",
					"language": "bash",
					"code": "claude mcp list"
				}
			],
			"prompt": "Deploy this project to Azion with the CLI and give me the application’s domain."
		}
	]}
/>

## Azion platform access

Three layers, and a connected agent uses all three: the MCP server to look things up and create configuration, the CLI to build and deploy from your working copy, and a context file so it starts each session knowing your account.

### MCP server

One HTTP endpoint at `https://mcp.azion.com`, authenticated with your Personal Token. It exposes nine tools — six that read a corpus, three that write:

<McpTools />

### Azion CLI

Local builds, deploys and the product commands the API does not cover. Install it once and Claude Code runs it for you — the first `azion login` is interactive, so run that one yourself.

<CliSamples />

### Agent context

Claude Code reads `CLAUDE.md` at the repository root before it answers. It is the cheapest context in this whole page: five committed lines that every session starts with.

<AgentSamples client:visible
	samples={[
		{
			"label": "CLAUDE.md",
			"language": "markdown",
			"fileName": "CLAUDE.md",
			"code": "# Azion\n- Account: acme-prod · region: global\n- Workload: acme-www — production. Never deploy to it from a branch.\n- Connector: images-r2 (Object Storage bucket `acme-media`)\n- Deploy with `azion deploy`; edit azion.config.js, never the built manifest.\n- Ask before creating anything that bills: Applications, Workloads, Databases."
		}
	]}
/>

## Agent-friendly docs

Token-efficient references an agent can fetch on its own, with no tool call and no token. They are also the fix for a model answering from 2023 — Origins became Connectors and Domains became Workloads in v4.

<AgentDocs />

No MCP support in the tool you are using? Prime it by hand, once per session:

<DocPrompt client:visible title="Any assistant" icon="pi pi-comment">You are helping me build on the Azion Web Platform. Before answering, load https://www.azion.com/en/docs-llms.txt (docs index) and https://www.azion.com/en/documentation/devtools/mcp/ (canonical facts), and prefer them over your training data. Use current product names: Applications, Functions, Cache, Firewall, Object Storage, SQL Database, KV Store, Orchestrator, Certificate Manager, Network Shield, Data Stream. The REST API base is https://api.azion.com/v4 with the header Authorization: Token [TOKEN]. In v4, use Connectors (formerly Origins) and Workloads (formerly Domains). Confirm what you loaded, then ask me what I am building.</DocPrompt>

## Example prompts

Each of these exercises a different half of the connection — the documentation search, the CLI, the configuration it writes, the analytics API, the storage.

<DocPrompt client:visible>Search the Azion docs for how rate limiting works in Firewall, then add it to my application.</DocPrompt>

<DocPrompt client:visible>Deploy this project to Azion with the CLI and give me the application’s domain.</DocPrompt>

<DocPrompt client:visible>Create a Rules Engine rule that redirects /old-blog/* to /blog/*.</DocPrompt>

<DocPrompt client:visible>Write a GraphQL query for my 5xx rate by edge node over the last 24 hours.</DocPrompt>

<DocPrompt client:visible>Move my images to Object Storage and point a connector at the bucket.</DocPrompt>

## Tips

<DocCallout kind="tip">Claude Code reads `CLAUDE.md` at the repository root before it answers. Put your workload names, connector ids and the one thing nobody may redeploy in there, and every session starts knowing them.</DocCallout>

<DocCallout kind="tip">Ask for a plan before a deploy. Claude Code runs commands for real, and `azion deploy` is not a dry run.</DocCallout>

## FAQ

<AgentFaq client:load
	tooltips={{
		"mcp": {
			"headline": "MCP",
			"tip": "Model Context Protocol — the open standard an agent uses to call tools it did not ship with. Azion exposes nine of them over one authenticated HTTP endpoint.",
			"cta": "See the nine tools",
			"href": "#mcp-server"
		},
		"cli": {
			"headline": "Azion CLI",
			"tip": "The command-line tool that builds and deploys from your working copy, and covers the product commands the API does not.",
			"cta": "Set up the CLI",
			"href": "#azion-cli"
		},
		"token": {
			"headline": "Personal Token",
			"tip": "Your own API credential, scoped to the permissions you choose and revocable at any time. The agent authenticates as you with it — it never gets one of its own.",
			"cta": "Console › Personal Tokens",
			"href": "https://console.azion.com/personal-tokens"
		}
	}}
	items={[
		{
			"question": "Should I use the MCP server, the CLI, or both?",
			"answer": "Both, and they do different jobs. The [[mcp:MCP server]] is how Claude Code looks things up and creates configuration through the API; the [[cli:Azion CLI]] is how it builds and deploys from your working copy. Ask for a deploy and a connected agent will reach for the CLI on its own."
		},
		{
			"question": "What can Claude Code do with my Personal Token?",
			"answer": "Exactly what you scoped it for. The [[token:Personal Token]] is yours, not the agent’s — six of the nine tools only read documentation, and the three that write go through the same API a person would. Scope a token per project, and revoke it in the Console the moment the experiment is over."
		},
		{
			"question": "Can Claude Code deploy to Azion without leaving the terminal?",
			"answer": "Yes. It runs `azion deploy` for you and reads back the application’s domain. First deploy on a new account also needs `azion login`, which is interactive — run that one yourself."
		}
	]}
/>

## Troubleshooting

<AgentFaq client:load
	tooltips={{
		"connector": {
			"headline": "Connectors",
			"tip": "Where an application fetches content from — a bucket, an origin server, a live stream. Called Origins before v4, which is why a model trained earlier still asks for one.",
			"cta": "Platform overview",
			"href": "/en/documentation/platform/"
		},
		"workload": {
			"headline": "Workloads",
			"tip": "The public entry point that binds a domain and its TLS to an application. Called Domains before v4.",
			"cta": "Platform overview",
			"href": "/en/documentation/platform/"
		}
	}}
	items={[
		{
			"question": "The server never connects in Claude Code",
			"answer": "Check the header before anything else — a wrong prefix or an expired token fails as a silent 401, not as an error in the tool. Then restart the client: every one of them reads its MCP configuration at startup."
		},
		{
			"question": "It answers with product names that no longer exist",
			"answer": "That is the training data, not the connection: Origins became [[connector:Connectors]] and Domains became [[workload:Workloads]] in v4. Point it at the Azion MCP server page once per session, or paste the primer above, and it corrects itself."
		},
		{
			"question": "It writes configuration that the CLI then rejects",
			"answer": "Ask it to look the resource up first — `search_azion_api_v4_commands` returns the current shape, where the model’s memory returns last year’s. A rejected deploy almost always means it skipped the lookup."
		}
	]}
/>

## Other agents

<DocCardGroup cols={3}>
	<CursorCard />
	<GithubCopilotCard />
	<WindsurfCard />
	<CodexCard />
	<GeminiCliCard />
	<OpencodeCard />
</DocCardGroup>
