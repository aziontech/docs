---
title: Azion CLI's personal tokens command and its subcommands
description: Manage your personal tokens through Azion CLI.
meta_tags: cli, next, javascript, astro, vue, react, edge computing
namespace: documentation_cli_personal_tokens
menu_namespace: cliMenuAlpha
permalink: /documentation/products/cli/personal-tokens/
---

See the command that enables the management of [personal tokens](/en/documentation/products/accounts/personal-tokens/) through the Azion CLI.

---

## create personal-token

### Usage

```sh
azion create personal-token --name "newToken" --expiration "9m" 
```

### Optional flags

#### in 

The `--in` option informs the file path to a JSON file containing all attributes of the personal token being created.

**Example**

```sh
{
    "name": "One day token",
    "expires_at": "9m"
}
```

#### help

The `--help` option displays more information about the `create` subcommand.

### Required flags when --in isn't informed

#### description 

The `--description` option informs the personal token's description.

#### expiration 

The `--expiration` option informs the personal token's expiration.

**Example** 

```sh 
azion create personal-token --name "xxtoken" --expiration "9m" // months
azion create personal-token --name "xxtoken" --expiration "9d" // days
azion create personal-token --name "xxtoken" --expiration "1y" // year
```

#### name

The `--name` option informs the personal token's name.

---

## list personal-token

### Usage

```sh
azion list personal-token [flags]
```

### Optional flags

#### details

The `--details` option displays all relevant fields when listing.

#### help

The `--help` option displays more information about the `list` subcommand.

--- 

## delete personal-token

### Usage

```sh
azion delete personal-token --id xxxxxx-xxxxx-xxxx-xxxxx-xxxx
```

### Required flags 

#### id

The `--id` option informs the unique identifier of the personal token being deleted.

#### Optional flags

#### help

The `--help` option displays more information about the `delete` subcommand.


---

import ContributorList from '~/components/ContributorList.astro'

**Contributors** <ContributorList>Contributor</ContributorList>
