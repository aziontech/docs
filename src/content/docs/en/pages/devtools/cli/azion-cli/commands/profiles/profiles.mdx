---
title: Azion CLI profiles
description: >-
  Manage your Azion CLI profiles to switch between different accounts
  and configurations easily.
permalink: /documentation/devtools/cli/profiles/
meta_tags: >-
  Azion CLI, profiles management, account switching, CLI configuration,
  multiple accounts
namespace: documentation_cli_profiles
menu_namespace: cliMenuAlpha
---

Manage your Azion CLI profiles to switch between different accounts and configurations easily.

## Usage

```bash
azion profiles [flags]
```

## Description

The `profiles` command displays information about the profiles you have configured in your Azion CLI. Profiles allow you to manage multiple accounts or environments, making it easy to switch between different configurations.

## Examples

```bash
$ azion profiles
```

## Flags

### help

The `-h` or `--help` option displays more information about the `profiles` command.

## Global flags

The following global flags are available for the `profiles` command:

| Flag | Type | Description | Default |
|------|------|-------------|---------|
| `-c, --config` | string | Sets the Azion configuration folder for the current command only, without changing persistent settings. | - |
| `-d, --debug` | - | Displays log at a debug level. | - |
| `--format` | string | Changes the output format passing the json value to the flag. | - |
| `-l, --log-level` | string | Sets the logging level: "debug", "info", or "error". | "info" |
| `--no-color` | - | Disables colored output. | - |
| `--out` | string | Exports the output to the given `<file_path/file_name.ext>`. | - |
| `-s, --silent` | - | Silences log completely; mostly used for automation purposes. | - |
| `--timeout` | int | Defines how much time in seconds the CLI will wait before timing out from the HTTP connection. | 50 |
| `-t, --token` | string | Saves a given Personal Token locally to authorize CLI commands. | - |
| `-y, --yes` | - | Answers all yes/no interactions automatically with yes. | - |

## Related commands

To manage profiles, use the following commands:

- **[Create profile](/documentation/devtools/cli/create-profile/)**: Create a new profile configuration.
- **[Delete profile](/documentation/devtools/cli/delete-profile/)**: Delete an existing profile configuration.