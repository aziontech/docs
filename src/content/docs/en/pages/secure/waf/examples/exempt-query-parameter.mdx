---
title: Exempt one query string parameter
description: >-
  Stop one internal rule firing on one named query string argument, and leave it
  firing everywhere else.
meta_tags: 'waf, exception, allowed rules, conditions, specific query string name, rule id, operator, contains, console, azion cli, api'
namespace: documentation_products_waf_examples_exempt_query_parameter
permalink: /documentation/secure/waf/examples/exempt-query-parameter/
---

import FrameBox from '@aziontech/webkit/frame-box'
import ItemList from '@aziontech/webkit/item-list'
import DocItem from '@aziontech/webkit/doc-item'
import DocSteps from '@aziontech/webkit/doc-steps'
import DocStep from '@aziontech/webkit/doc-step'
import Tabs from '~/components/webkit/Tabs.vue'

An exception takes one part of a request out of the scoring of one internal rule. The one below stops rule `1013`, the apostrophe rule, firing on the `q` argument of `/search`, and leaves it firing on every other argument, path, and header. Reach for it when Tuning or Real-Time Events shows a legitimate search term blocked by a single rule.

Select the interface you will use. The snippet below follows that choice.

<Tabs client:visible sharedStore="interface">
<Fragment slot="tab.console">Console</Fragment>
<Fragment slot="tab.cli">CLI</Fragment>
<Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">

<DocSteps>
  <DocStep title="Open the WAF Rules page">

  Access [Azion Console](https://console.azion.com/) > **Edge Libraries** > **WAF Rules**.

  </DocStep>
  <DocStep title="Select the rule set that blocked the request" />
  <DocStep title="Open the Allowed Rules tab and select + Allowed Rule" />
  <DocStep title="Select the rule to exempt">

  In **Rule ID**, select `1013`.

  </DocStep>
  <DocStep title="Describe the exception and restrict it to one path">

  In **Description**, enter `Allow apostrophes in the storefront search term`. In **Path**, enter `/search`.

  </DocStep>
  <DocStep title="Name the part of the request">

  In **Condition**, select _Specific Query String Name_ and enter `q` in the **Name** field it reveals. In **Operator**, select `contains`.

  </DocStep>
  <DocStep title="Save the exception" />
</DocSteps>

For the same exception with every field it accepts explained, refer to [Create a WAF exception](/en/documentation/guides/application-security/firewall-and-waf/configure-waf-allowed-rules/).

</Fragment>

<Fragment slot="panel.cli">

Save the exception as `exception.json`:

```json
{
  "rule_id": 1013,
  "name": "Allow apostrophes in the storefront search term",
  "path": "/search",
  "operator": "contains",
  "active": true,
  "conditions": [
    { "match": "specific_query_string_name", "name": "q" }
  ]
}
```

The Azion CLI takes the same file:

```bash
azion create waf-exceptions --waf-id <waf-id> --file exception.json
```

```text
Created WAF Exception with ID 281875
```

Use `--file` for the CLI form. On Azion CLI 4.23.0 the `--conditions` flag never reaches the request: whatever value you pass, the command sends an empty array and the API refuses it with `10049 Min Length List Field` pointing at `/data/conditions`.

</Fragment>

<Fragment slot="panel.api">

Save the exception as `exception.json`:

```json
{
  "rule_id": 1013,
  "name": "Allow apostrophes in the storefront search term",
  "path": "/search",
  "operator": "contains",
  "active": true,
  "conditions": [
    { "match": "specific_query_string_name", "name": "q" }
  ]
}
```

Post it to the rule set that blocked the request:

```bash
curl --request POST \
  --url https://api.azion.com/v4/workspace/wafs/<waf-id>/exceptions \
  --header 'Accept: application/json' \
  --header 'Authorization: Token [TOKEN VALUE]' \
  --header 'Content-Type: application/json' \
  --data @exception.json
```

The API answers `202` and echoes the condition under `data.conditions`, unchanged.

</Fragment>

</Tabs>

## How it works

The match value decides which extra key the condition carries. `specific_query_string_name` names one argument, so it takes a `name`. Its generic counterpart, `any_query_string_name`, takes `match` alone and covers every argument in the request. Two keys default wider than a false positive usually needs: `rule_id` defaults to `0`, which is every internal rule, and `operator` defaults to `contains`, which treats `name` as a substring rather than a pattern.

## Related resources

<FrameBox>
<ItemList>
  <DocItem title="WAF examples" href="/en/documentation/secure/waf/examples/">Browse the rest of the snippets in this collection.</DocItem>
  <DocItem title="Exempt one request header" href="/en/documentation/secure/waf/examples/exempt-request-header/">The same shape for a header, and the condition that silently covers all of them.</DocItem>
  <DocItem title="WAF Exceptions" href="/en/documentation/secure/waf/custom-allowed-rules/">Every field an exception carries, and the fifteen match values a condition takes.</DocItem>
  <DocItem title="Create a WAF exception" href="/en/documentation/guides/application-security/firewall-and-waf/configure-waf-allowed-rules/">The steps that build this exception in Azion Console instead.</DocItem>
  <DocItem title="Read the score of a blocked request" href="/en/documentation/secure/waf/examples/read-block-score/">Find the internal rule to name in `rule_id` before you write the exception.</DocItem>
</ItemList>
</FrameBox>
