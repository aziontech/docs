diff --git a/src/components/documentation/doc-page-header/doc-page-header.vue b/src/components/documentation/doc-page-header/doc-page-header.vue
index b702b197ce628cee55bf09ad79f871ddafab2f2b..f9ec6c203882994765b87fa02017c7668620daf1 100644
--- a/src/components/documentation/doc-page-header/doc-page-header.vue
+++ b/src/components/documentation/doc-page-header/doc-page-header.vue
@@ -92,6 +92,10 @@
     breadcrumb?: DocCrumb[]
     /** When the page's content last changed. ISO date, or a ready-made string. */
     lastUpdated?: string
+    /** The words before the date, for a site that does not read English. */
+    lastUpdatedLabel?: string
+    /** BCP 47 tag the ISO date is formatted in. */
+    locale?: string
     /** The controls on the meta line, in reading order. */
     metaActions?: DocPageAction[]
   }
@@ -101,6 +105,8 @@
     description: '',
     breadcrumb: () => [],
     lastUpdated: '',
+    lastUpdatedLabel: 'Last updated',
+    locale: 'en-US',
     metaActions: () => []
   })
 
@@ -142,7 +148,7 @@
     if (!/^\d{4}-\d{2}-\d{2}$/.test(raw)) return raw
     const parsed = new Date(`${raw}T00:00:00Z`)
     if (Number.isNaN(parsed.getTime())) return raw
-    return new Intl.DateTimeFormat('en-US', {
+    return new Intl.DateTimeFormat(props.locale, {
       month: 'short',
       day: 'numeric',
       year: 'numeric',
@@ -235,7 +241,7 @@
           aria-hidden="true"
         />
         <span>
-          Last updated
+          {{ lastUpdatedLabel }}
           <time :datetime="lastUpdated">{{ updatedLabel }}</time>
         </span>
       </p>
diff --git a/src/components/documentation/doc-prose/doc-prose.vue b/src/components/documentation/doc-prose/doc-prose.vue
index e4b0c7a444af99d276fd30dbfc30f2854f4ffe59..6d09ea10875742664e0ec6b2568c38c6745ef9e0 100644
--- a/src/components/documentation/doc-prose/doc-prose.vue
+++ b/src/components/documentation/doc-prose/doc-prose.vue
@@ -347,6 +347,19 @@
     globalThis.addEventListener('resize', reanchor, { passive: true })
   })
 
+  // The tip teleports to <body>, and an SSR host that does not re-emit Vue's teleport
+  // payload into the served markup (Astro is one) leaves the client hydrating the
+  // page's own first body child where it expects the teleport anchor -- a hydration
+  // node mismatch on every documentation page that hydrates this component. Teleporting
+  // only once mounted keeps the server render and the client's first render identical:
+  // the tip is a v-if comment inline in the root either way. Nothing is lost by
+  // waiting -- the tip only ever opens on pointerover, which is after mount.
+  const isMounted = ref(false)
+
+  onMounted(() => {
+    isMounted.value = true
+  })
+
   onBeforeUnmount(() => {
     const root = proseRef.value
     root?.removeEventListener('pointerover', onOver)
@@ -382,7 +395,10 @@
          silently drops the `class` the consumer passes to cap the column.
          Nested here it still renders under `<body>`; the component just keeps
          its single root. -->
-    <Teleport to="body">
+    <Teleport
+      to="body"
+      :disabled="!isMounted"
+    >
       <Transition
         enter-active-class="animate-popup-scale-in motion-reduce:animate-none"
         leave-active-class="animate-popup-scale-out motion-reduce:animate-none"
diff --git a/src/components/documentation/doc-step/doc-step.vue b/src/components/documentation/doc-step/doc-step.vue
index 5507693153daad0cb9557b2c120314bd7bb8aea5..97c9e7b746ce16592acb8f32601cd37a3c948329 100644
--- a/src/components/documentation/doc-step/doc-step.vue
+++ b/src/components/documentation/doc-step/doc-step.vue
@@ -1,7 +1,7 @@
 <script setup lang="ts">
-  import { computed, onBeforeUnmount, useAttrs } from 'vue'
+  import { computed, inject, onBeforeUnmount, useAttrs } from 'vue'
 
-  import { useDocStepsContext } from '../doc-steps/composables/use-doc-steps-context'
+  import { DocStepsInjectionKey } from '../doc-steps/injection-key'
 
   /**
    * One numbered step: the circled index, the rail that continues to the next
@@ -31,15 +31,17 @@
 
   const attrs = useAttrs()
 
-  const context = useDocStepsContext()
+  // Rendered as its own root (one component per MDX node) there is no provider;
+  // the number then comes from a CSS counter on the empty badge.
+  const context = inject(DocStepsInjectionKey, null)
 
   const id = Symbol('DocStep')
-  const { index } = context.register(id)
+  const index = context ? context.register(id).index : null
 
   // A consumer-supplied data-testid wins; otherwise the derived fallback.
   const testId = computed(() => (attrs['data-testid'] as string) ?? 'documentation-doc-step')
 
-  onBeforeUnmount(() => context.unregister(id))
+  onBeforeUnmount(() => context?.unregister(id))
 </script>
 
 <template>
@@ -51,10 +53,9 @@
     <div class="flex w-8 shrink-0 flex-col items-center">
       <span
         class="flex size-8 shrink-0 items-center justify-center rounded-(--radius-full) border border-(--border-default) bg-(--bg-surface) text-label-md text-(--text-default)"
+        data-step-number
         aria-hidden="true"
-      >
-        {{ index }}
-      </span>
+      >{{ index ?? '' }}</span>
       <span
         data-step-connector
         class="w-px flex-1 bg-(--border-default)"
diff --git a/src/components/documentation/doc-steps/doc-steps.vue b/src/components/documentation/doc-steps/doc-steps.vue
index a09a9e7ae4197002273e09e0c8234d00fa5f767c..0746678da70a70b8abedab85461773ef82a7a666 100644
--- a/src/components/documentation/doc-steps/doc-steps.vue
+++ b/src/components/documentation/doc-steps/doc-steps.vue
@@ -54,6 +54,7 @@
   <div
     v-bind="$attrs"
     data-doc-block
+    data-doc-steps
     :data-testid="testId"
     class="flex w-full flex-col items-start"
   >
diff --git a/src/components/navigation/dropdown/dropdown.vue b/src/components/navigation/dropdown/dropdown.vue
index e4ed71685532b6c7e9ff43509c30082c6fb4283d..e7c8c4e92cf91f833e6d1bdeccb426b8f361309d 100644
--- a/src/components/navigation/dropdown/dropdown.vue
+++ b/src/components/navigation/dropdown/dropdown.vue
@@ -281,7 +281,13 @@
     }
   )
 
-  onMounted(() => {
+    // The overlay teleports to <body>, and an SSR host that does not re-emit Vue's
+  // teleport payload into the served markup leaves the client hydrating the wrong
+  // node. Teleporting only once mounted keeps both renders identical.
+  const isMounted = ref(false)
+
+onMounted(() => {
+    isMounted.value = true
     globalThis.document?.addEventListener('mousedown', onDocumentMousedown)
     globalThis.window?.addEventListener('keydown', onWindowKeydown)
   })
@@ -330,7 +336,10 @@
   >
     <slot />
 
-    <Teleport to="body">
+    <Teleport
+      to="body"
+      :disabled="!isMounted"
+    >
       <Transition
         enter-active-class="animate-popup-scale-in motion-reduce:animate-none"
         leave-active-class="animate-popup-scale-out motion-reduce:animate-none"
diff --git a/src/components/navigation/menu/menu-sub-trigger/menu-sub-trigger.vue b/src/components/navigation/menu/menu-sub-trigger/menu-sub-trigger.vue
index 7cb4625ba4726404505ed6c1bd047377e553ab00..86395b8b75a01dcf81470fea37007b0f9b57f95b 100644
--- a/src/components/navigation/menu/menu-sub-trigger/menu-sub-trigger.vue
+++ b/src/components/navigation/menu/menu-sub-trigger/menu-sub-trigger.vue
@@ -49,7 +49,9 @@
    * replaces the menu — so it sits amongst the leaves it is listed with and takes an icon for
    * the same reason they do.
    */
-  const showIcon = computed(() => Boolean(props.icon) && !isInline.value)
+  // The docs sidebar puts a glyph on the rows that group products, inline or not,
+  // so the trigger renders whatever icon it is handed.
+  const showIcon = computed(() => Boolean(props.icon))
 
   // Reads as a MENU ROW, at the rows' own size and colour. Only a first-level group title is
   // allowed to be smaller and muted; from there every row is 14px, a trigger included — a row
diff --git a/src/components/navigation/tab-view/tab-view-panel.vue b/src/components/navigation/tab-view/tab-view-panel.vue
index 607875d360f1e8fca6f6bef35dffbccac38a087c..f8fd34bd6f6652d5301ae3c9c6b8ad377f3d2778 100644
--- a/src/components/navigation/tab-view/tab-view-panel.vue
+++ b/src/components/navigation/tab-view/tab-view-panel.vue
@@ -69,6 +69,10 @@
   const runEnterMotion = () => {
     motionReady.value = false
     nextTick(() => {
+      if (typeof globalThis.requestAnimationFrame !== 'function') {
+        motionReady.value = true
+        return
+      }
       globalThis.requestAnimationFrame(() => {
         motionReady.value = true
       })
diff --git a/src/components/overlay/tooltip/tooltip.vue b/src/components/overlay/tooltip/tooltip.vue
index 89a09dd58626d9dcabecb0be82958c45cb2f68b3..664801290f709c4d604682e62e441e34ffbca1af 100644
--- a/src/components/overlay/tooltip/tooltip.vue
+++ b/src/components/overlay/tooltip/tooltip.vue
@@ -128,7 +128,13 @@
     }
   )
 
-  onMounted(() => {
+    // The overlay teleports to <body>, and an SSR host that does not re-emit Vue's
+  // teleport payload into the served markup leaves the client hydrating the wrong
+  // node. Teleporting only once mounted keeps both renders identical.
+  const isMounted = ref(false)
+
+onMounted(() => {
+    isMounted.value = true
     globalThis.document?.addEventListener('keydown', onDocumentKeydown)
   })
 
@@ -156,7 +162,10 @@
     <slot />
   </span>
 
-  <Teleport to="body">
+  <Teleport
+      to="body"
+      :disabled="!isMounted"
+    >
     <Transition
       enter-active-class="animate-popup-scale-in motion-reduce:animate-none"
       leave-active-class="animate-popup-scale-out motion-reduce:animate-none"
